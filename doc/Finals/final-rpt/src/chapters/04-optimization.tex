\section{性能与时序优化}\label{sec:optimization}

\subsection{优化方法}

优化过程把程序运行时间和实现时序作为一组联合指标：首先从完整工作负载的周期、停顿、分支与访存
统计中定位高频事件，再对 Vivado 最差路径的逻辑级数、布线占比和扇出进行结构归因，最后以回归测试、
实现报告和真实板测共同评价。\textbf{性能数据只在相同正式负载、相同迭代规模和正确 CRC 之间比较}；WNS
记录与产生对应 bitstream 的实现一一配对。

\input{src/tikz/optimization-loop}

\subsection{前端控制流优化}

控制流优化围绕“查询深度、容量冲突、后端修正开销”同步展开。BTB 扩展为 512 项后，采用 8 个 64 项
bank 分摊异步查询深度；valid、tag、目标地址与 2-bit 方向状态共同决定预测。更新端使用窄 shadow
保存更新所需信息，避免后端写入信息经宽比较网络回绕到当前取指拍。条件分支按局部饱和计数器判断，
JAL 和 return 直接采用目标，return 可由两项 RAS 消除 BTB 目标别名。

后端一旦发现预测方向或目标不一致，即生成一次重定向事件。所谓\textbf{重定向邮箱}，是指一组保存
待处理目标地址和有效位的寄存器：当 IFU 当拍受存储响应或下游反压影响而不能立即接收新 PC 时，EXU
产生的目标不会丢失，而是写入邮箱；IFU 接收后清除有效位。重定向后的第一条取指采用顺序下一 PC，
随后恢复正常预测，从而切断“后端比较---重定向---异步 BTB 查询---下一 PC”在同一拍内形成的组合链。
预测、校验、更新与冲刷的完整流程见第\ref{sec:frontend}节。

\subsection{缓存与 load-use 优化}

数据缓存由 2\,KiB 扩展为 4\,KiB 双 bank，以容量和 bank 选择共同降低直接映射冲突。主数据使用
同步 BRAM，逐字节 LUTRAM 副本提供异步窄加载值。\textbf{late-load 并非把任意 load 数据广播到所有消费者}，
而是仅向已定义的短路径消费者提供一个带 valid、目的寄存器号、地址低位和扩展后数据的旁路 token。
普通消费者仍等待 LSU/WBU 的顺序结果；被允许的相邻消费者在 token 命中时提前取得数据。

\input{src/tikz/late-load}

图\ref{fig:late-load}中，查询副本与主 BRAM 接收相同的有效写入，因而窄加载值和主数据保持一致。
分支比较、地址生成等消费者分别在自己的入口选择 late-load token，选择条件不进入 D-cache tag/data
写入路径。store 能完整覆盖一个字时直接更新并分配缓存行；窄 store 无法安全合并时使对应行失效；
store epoch 阻止较早 load 的延迟回填覆盖较新的写入。

\subsection{执行和后端通路优化}

普通整数 ALU、直接结果、长算术、硬件加速和 load 结果划分为五类。相邻快速结果采用分组编码选择，
其控制信号不进入地址、M/B、自定义指令和 CSR 等长路径。地址操作数单独注册，D-cache 查询只携带
所需 index/tag；加速单元的请求、响应和结果分别设置边界寄存。该结构让每类消费者只观察必需的数据
与有效标志，减小宽选择器、扇出和跨模块组合回路。

\subsection{乘法与位操作优化}

普通低位乘法使用较短 DSP 流水；操作数满足定宽条件时进入一周期窄乘法通路，高位乘法仍使用完整
有符号或无符号乘积。部分 RISC-V B 扩展按逻辑深度分为短组合操作和迭代操作，避免简单的移位加法
或符号扩展承担通用迭代延迟。多周期指令在结果未就绪期间持续提供目的寄存器占用信息，使 IDU 对
相关消费者保持停顿；结果有效后才允许旁路或提交。

\subsection{总决赛阶段完整性能与 WNS 演进}\label{sec:performance}

表\ref{tab:final-full-evolution}保留从 300\,MHz 前端扩容开始，到自定义指令与后端边界整理完成后的
板测成功记录。P0--P7 使用同一 10000 次正式负载且运行时间保持不变，重点表现结构调整如何逐级改善
WNS；P8 以后继续采用 10000 次正式负载和四项正确 CRC，但程序能力随已保留的指令与硬件结构演进，
因此同时列出“结构变化”以限定比较含义。所有时间均来自原始 UART 计数结果。

\begin{table}[H]
\centering
\caption{总决赛阶段板测性能与 WNS 完整演进}\label{tab:final-full-evolution}
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabularx}{\linewidth}{@{}C{0.045\linewidth}C{0.085\linewidth}Y C{0.13\linewidth}C{0.11\linewidth}@{}}
\toprule
\textbf{点}&\textbf{短提交号}&\textbf{保留的结构改进}&\textbf{板测时间/s}&\textbf{WNS/ns}\\\midrule
P0&\code{851d51c}&512 项 BTB&10.695831&$-0.776$\\
P1&\code{1b73639}&BTB valid 并入 LUTRAM&10.695831&$-0.764$\\
P2&\code{7941852}&BTB 两 bank 与更新写流水&10.695831&$-0.677$\\
P3&\code{677d6fa}&BTB 四 bank&10.695831&$-0.571$\\
P4&\code{fb6496e}&BTB 8-bank&10.695831&$-0.550$\\
P5&\code{0358870}&EXU 写回选择器拍平&10.695831&$-0.507$\\
P6&\code{5728ae3}&ALU 结果选择局部化&10.695831&$-0.495$\\
P7&\code{5728ae3}&同一结构独立实现复测&10.695831&$-0.452$\\
P8&\code{2b04bbd}&D-cache tag 缩减、late-load 注册与地址通路缩短&10.078266&$-0.285$\\
P9&\code{f1218e5}&单周期 CRC 字节更新指令&9.565066&$-0.534$\\
P10&\code{a3b090b}&定宽位域乘法进入单周期结果通路&9.077274&$-0.520$\\
P11&\code{b680bff}&顺序访存矩阵归约单元&8.572723&$-0.733$\\
P12&\code{dbda89f}&字输入、分拍处理的流式状态单元&7.745942&$-0.684$\\
P13&\code{bdd8f0e}&链式结构单步变换与存储一致性控制&7.516036&$-0.745$\\
P14&\code{4853eed}&链式循环融合和提交边界整理&7.160581&$-0.785$\\
P15&\code{67b5d42}&硬件加速指令组合及镜像布局整理&7.106449&$-0.615$\\
P16&\code{66f8fd5}&流式状态计数归约下沉到硬件&6.696372&$-0.733$\\
P17&\code{6ff30cd}&乘法 IP 输出一致性修复与板级复测&6.714951&$-0.696$\\
P18&\code{fabb599}&当前后端结果分区、控制流与缓存结构整合&6.745018&$-0.580$\\
\bottomrule
\end{tabularx}
\end{table}

WNS 取对应实现可用的最终 post-route 或 post-route physical optimization 结果；P15 的程序镜像在
相同物理实现上调整后复测，因此沿用该 bitstream 的 WNS。折线采用独立左右轴，蓝线观察性能，红线
观察建立时序余量，二者不进行数值尺度上的相互比较。

\input{src/tikz/finals-performance-wns}

\subsubsection{关键里程碑提炼}

完整演进中可归纳出八个对体系结构或性能产生显著影响的里程碑，如表\ref{tab:major-milestones}所示。
“阶段收益”均以同口径的前一里程碑时间计算，WNS 则用于观察性能改进对物理实现的影响。

\begin{table}[H]\centering
\caption{总决赛阶段关键优化里程碑}\label{tab:major-milestones}
\small
\begin{tabularx}{\linewidth}{@{}p{0.16\linewidth}Y C{0.14\linewidth}C{0.12\linewidth}@{}}
\toprule
\textbf{提交/点}&\textbf{关键里程碑}&\textbf{板测时间/s}&\textbf{WNS/ns}\\\midrule
\code{851d51c}/P0&512 项 BTB 建立容量基线&10.695831&$-0.776$\\
\code{5728ae3}/P7&8-bank、更新流水和结果选择局部化&10.695831&$-0.452$\\
\code{2b04bbd}/P8&缓存、late-load 与窄地址路径协同&10.078266&$-0.285$\\
\code{f1218e5}/P9&CRC 字节更新进入单周期数据通路&9.565066&$-0.534$\\
\code{b680bff}/P11&位域乘法与矩阵归约协同&8.572723&$-0.733$\\
\code{dbda89f}/P12&流式状态处理改为字输入分拍结构&7.745942&$-0.684$\\
\code{67b5d42}/P15&链式变换与硬件加速指令组合完成&7.106449&$-0.615$\\
\code{66f8fd5}/P16&状态计数归约下沉，形成最佳板测点&6.696372&$-0.733$\\
\bottomrule
\end{tabularx}
\end{table}

\input{src/tikz/finals-major-milestones}

\textbf{从 P0 到 P16，板测时间由 10.695831\,s 降至 6.696372\,s，缩短约 37.39\%}。P0--P7 在性能不变的
条件下将 WNS 改善 324\,ps，说明 bank 化和结果通路局部化主要贡献于物理结构；P8 以后通过减少动态
指令、缩短高频数据依赖和保持存储一致性取得主要性能收益。P17 使用经校核的乘法 IP 输出产物重新
实现并完成板测，性能为 6.71495094\,s，作为正确 IP 配置下的独立板级复测点。P18 则以当前设计、
正式 10,000 次程序镜像和经校核的一周期乘法 IP 重新生成 bitstream；四项 CRC 均正确，板测时间为
\textbf{6.74501768\,s}，对应 routed WNS 为 $-0.590$\,ns，post-route physical optimization WNS 为 $-0.580$\,ns。

\subsection{当前实现频率与 WNS}\label{sec:timing}

当前设计在 250\,MHz 下 routed WNS 为 $-0.087$\,ns，经 post-route physical optimization 后达到
\textbf{$+0.009$\,ns，完成建立时序闭合}\evidence{E-FINAL-250}。280\,MHz 实现取得 routed WNS
$-0.664$\,ns\evidence{E-FINAL-280}。300\,MHz 实现的 routed WNS 为 $-0.590$\,ns，post-route
physical optimization 为 $-0.580$\,ns\evidence{E-FINAL-300}；该频率点已完成布局布线，但仍有
setup 时序缺口。该 300\,MHz 实现的精确重建 bitstream 已完成真实板测：10,000 次迭代用时
\textbf{6.74501768\,s}，CRC 为 \code{e714/1fd7/8e3a/988c}。

\begin{table}[H]\centering
\caption{当前设计在不同频率下的实现结果}
\small
\begin{tabularx}{\linewidth}{@{}C{0.13\linewidth}C{0.19\linewidth}C{0.22\linewidth}Y@{}}\toprule
\textbf{频率}&\textbf{Routed WNS}&\textbf{Post-physopt WNS}&\textbf{实现状态}\\\midrule
250 MHz&$-0.087$ ns&$+0.009$ ns&建立时序闭合\\
280 MHz&$-0.664$ ns&---&完成 routed 结果记录\\
300 MHz&$-0.590$ ns&$-0.580$ ns&完成实现，继续进行结构级时序优化\\
\bottomrule\end{tabularx}
\end{table}

\input{src/tikz/frequency-wns}

\PlaceholderFigure{250 MHz 时序报告截图}{请插入 post-route physical optimization Timing Summary 的关键区域。}
\PlaceholderFigure{300 MHz 时序报告截图}{请并列插入 routed 与 post-route physical optimization Timing Summary 的关键区域。}

\subsection{工程可复现性}

实现脚本统一完成 SystemVerilog 生成与打包、Vivado 工程源刷新、IP/OOC 生成、布局布线、报告提取和
归档。流程显式记录程序镜像身份、请求与实际频率、实现命令、关键报告和板测原始输出；顶层实现并发
不低于 16，IP/OOC 并发不低于 4。对内联仿真模型，仿真保留模型，综合与打包阶段排除模型并绑定
同名 Vivado IP，确保仿真接口、流水时延与物理实现配置一致。
