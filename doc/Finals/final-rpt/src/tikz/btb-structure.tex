\begin{figure}[H]\centering\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
 b/.style={draw,rounded corners=1.2pt,align=center,minimum height=0.82cm,fill=blue!6},
 mem/.style={draw,rounded corners=1.2pt,align=center,minimum width=1.35cm,minimum height=2.15cm,fill=green!7},
 a/.style={-Stealth,thick},font=\small]
% 顶部主要数据流：PC -> 字段拆分 -> 8-bank 阵列 -> 命中选择。
\node[b,minimum width=1.8cm] (pc) {推测 PC};
\node[b,right=0.6cm of pc,minimum width=2.4cm] (split) {\texttt{pc[16:2]}\\tag / bank / 行号};

\node[mem,right=0.8cm of split] (m0) {bank 0\\64 项\\异步读};
\foreach \i [remember=\i as \last (initially 0)] in {1,...,7}{
  \node[mem,right=-0.01cm of m\last] (m\i) {bank \i\\64 项\\异步读};
}
\node[draw,dashed,rounded corners=1.2pt,fit=(m0)(m7),inner sep=5pt,
      label=above:{512 项查询阵列}] (array) {};
\node[b,right=0.8cm of array,minimum width=2.5cm] (pick) {bank 选择\\tag 相等且类型有效};

\draw[a] (pc)--(split);
\draw[a] (split)--(array.west);
\draw[a] (array.east)--(pick);

% 下方控制与预测汇聚逻辑。
\node[b,below=0.8cm of pick,minimum width=2.5cm,fill=orange!8] (decode) {类型译码\\JAL / branch / return};
\node[b,left=1.2cm of decode,minimum width=2.8cm,fill=cyan!7] (ras) {2 项返回地址栈\\call 压入，return 弹出};
\node[b,left=0.4cm of ras,minimum width=2.8cm,fill=purple!7] (counter) {表项 2-bit 饱和计数器\\最高位给出 branch 方向};
\node[b,below=1.0cm of decode,minimum width=3.4cm,fill=gray!10] (next) {选择 BTB 目标 / RAS 栈顶 / $pc+4$\\形成 PredBundle};

\draw[a] (pick)--(decode);
\draw[a] (decode)--(next);
\draw[a] (counter.south) |- (next.west);
\draw[a] (ras.south) |- ([yshift=0.2cm]next.west);
\end{tikzpicture}}\caption{512 项八 bank BTB 与返回预测结构}\end{figure}
