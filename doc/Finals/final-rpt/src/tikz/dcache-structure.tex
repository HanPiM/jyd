\begin{figure}[H]\centering\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
 m/.style={draw,rounded corners=1.2pt,align=center,minimum height=1.05cm,inner sep=5pt},
 a/.style={-Stealth,thick},wa/.style={-Stealth,thick,dashed},font=\footnotesize]
\node[m,fill=blue!6,text width=2.5cm] (addr) at (0,0.5) {访问地址\\tag=[17:11]\\bank=[11]，行=[10:2]};

\node[m,fill=green!7,text width=2.8cm] (tags) at (4.2,2.8) {双 bank tag LUTRAM\\valid + 7-bit tag\\C0 组合读与比较};
\node[m,fill=green!7,text width=3.0cm] (main) at (8.8,2.8) {双 bank 主数据 BRAM\\每 bank 512$\times$32 bit\\C1 同步读};
\node[m,fill=gray!10,text width=2.9cm] (normal) at (13.2,2.8) {普通命中通路\\延迟 bank 选择\\LSU 取得数据并前递};

\node[m,fill=orange!8,text width=3.25cm] (shadow) at (4.2,-1.6) {异步数据镜像\\每 bank 四个 512$\times$8 LUTRAM\\C0 读出原始 32-bit 字};
\node[m,fill=cyan!8,text width=3.1cm] (late) at (8.8,-1.6) {EXU--LSU 寄存边界\\注册 offset / 宽度 / 原始数据};
\node[m,fill=cyan!8,text width=3.0cm] (consumer) at (13.2,-1.6) {late-load 受限消费者\\C1 扩展后运算或比较};

\node[m,fill=purple!7,text width=4.7cm] (write) at (-0.2,-5.0) {统一缓存写入\\整字分配 / 窄写失效 / miss 回填 / 硬件加速更新};
\node[m,fill=purple!3,text width=4.5cm] (broadcast) at (5.8,-5.0) {同一地址、数据与字节掩码\\同步广播到 tag、主 BRAM 与镜像};

\draw[a] (addr.north east) -- (tags.west);
\draw[a] (tags)--node[above]{hit}(main);
\draw[a] (main)--(normal);
\draw[a] (addr.south east) -- (shadow.west);
\draw[a] (tags.south) -- node[right]{tag 命中放行} (shadow.north);
\draw[a] (shadow)--(late);
\draw[a] (late)--(consumer);
\draw[a] (write)--(broadcast);

% 三类写目标沿不同外侧通道布线，不穿越查询和数据节点。
\draw[wa] (broadcast.north) -- ++(0,0.55) -| (shadow.south);
\draw[wa] (broadcast.west) -- ++(-7.0,0) |- ([yshift=0.7cm]tags.north) -- (tags.north);
\draw[wa] (broadcast.east) -- ++(8.7,0) |- ([yshift=0.7cm]main.north) -- (main.north);
\end{tikzpicture}}
\caption{4 KiB 双 bank D-cache：普通命中通路与 late-load 异步镜像}\end{figure}
