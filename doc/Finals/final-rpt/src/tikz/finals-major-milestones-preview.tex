\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      width=0.88\textwidth,height=5.6cm,
      symbolic x coords={BTB512,通路局部化,缓存同步,CRC字节更新,定宽访存,流式分拍,硬件组合,流式提交,加速接口配置,固定取指槽,依赖累加,同步load边界,最终构建配置},
      xtick={BTB512,通路局部化,缓存同步,CRC字节更新,定宽访存,流式分拍,硬件组合,流式提交,加速接口配置,固定取指槽,依赖累加,同步load边界,最终构建配置},
      x tick label style={rotate=28,anchor=east,font=\scriptsize},
      ylabel={10,000 次时间（s）},ymin=5.0,ymax=11.1,
      axis y line*=left,axis x line*=bottom,grid=major,
      legend style={at={(0,1.02)},anchor=south west,font=\scriptsize}]
      \addplot[blue!75!black,mark=*] coordinates {
        (BTB512,10.695831) (通路局部化,10.695831) (缓存同步,10.078266)
        (CRC字节更新,9.565066) (定宽访存,8.572723) (流式分拍,7.745942)
        (硬件组合,7.106449) (流式提交,6.696372) (加速接口配置,6.691154)
        (固定取指槽,5.855369) (依赖累加,5.346451)};
      \addlegendentry{板测时间}
      \addplot[cyan!65!blue,dashed,mark=triangle*] coordinates {
        (依赖累加,5.346451) (同步load边界,5.352969) (最终构建配置,5.292462)};
      \addlegendentry{未上板拟合}
    \end{axis}
    \begin{axis}[
      width=0.88\textwidth,height=5.6cm,
      symbolic x coords={BTB512,通路局部化,缓存同步,CRC字节更新,定宽访存,流式分拍,硬件组合,流式提交,加速接口配置,固定取指槽,依赖累加,同步load边界,最终构建配置},
      xtick=\empty,ylabel={WNS（ns）},ymin=-1.5,ymax=-0.20,
      axis y line*=right,axis x line=none,
      legend style={at={(1,1.02)},anchor=south east,font=\scriptsize}]
      \addplot[red!75!black,dashed,mark=square*] coordinates {
        (BTB512,-0.776) (通路局部化,-0.452) (缓存同步,-0.285)
        (CRC字节更新,-0.534) (定宽访存,-0.733) (流式分拍,-0.684)
        (硬件组合,-0.615) (流式提交,-0.733) (加速接口配置,-0.580)
        (固定取指槽,-0.772) (依赖累加,-1.058) (同步load边界,-1.379)
        (最终构建配置,-0.699)};
      \addlegendentry{WNS}
    \end{axis}
  \end{tikzpicture}
  \caption*{关键优化里程碑的板测/拟合时间与 WNS 走向}
\end{figure}
