diff --git a/gcc/config/riscv/bitmanip.md b/gcc/config/riscv/bitmanip.md
index 0d16d79df..6c75513b1 100644
--- a/gcc/config/riscv/bitmanip.md
+++ b/gcc/config/riscv/bitmanip.md
@@ -1217,6 +1217,26 @@
   [(set_attr "type" "clmul")])
 
 ;; Reversed CRC 8, 16, 32 for TARGET_64
+(define_insn "byte_crc16_hi"
+  [(set (match_operand:HI 0 "register_operand" "=r")
+	(unspec:HI [(match_operand:HI 1 "register_operand" "r")
+		    (match_operand:QI 2 "register_operand" "r")]
+		   UNSPEC_XCRCU8))]
+  "riscv_xcrcu8 && !TARGET_64BIT"
+  ".insn r 0x0b, 0, 0, %0, %2, %1"
+  [(set_attr "type" "bitmanip")
+   (set_attr "mode" "HI")])
+
+(define_insn "byte_crc16_si"
+  [(set (match_operand:SI 0 "register_operand" "=r")
+	(unspec:SI [(match_operand:SI 1 "register_operand" "r")
+		    (match_operand:QI 2 "register_operand" "r")]
+		   UNSPEC_XCRCU8))]
+  "riscv_xcrcu8 && !TARGET_64BIT"
+  ".insn r 0x0b, 0, 0, %0, %2, %1"
+  [(set_attr "type" "bitmanip")
+   (set_attr "mode" "SI")])
+
 (define_expand "crc_rev<ANYI1:mode><ANYI:mode>4"
 	;; return value (calculated CRC)
   [(set (match_operand:ANYI 0 "register_operand")
@@ -1230,6 +1250,17 @@
   /* We don't support the case when data's size is bigger than CRC's size.  */
   "<ANYI:MODE>mode >= <ANYI1:MODE>mode"
 {
+  if (riscv_xcrcu8 && <ANYI1:MODE>mode == QImode
+      && (UINTVAL (operands[3]) & 0xffff) == 0x8005
+      && (<ANYI:MODE>mode == HImode || <ANYI:MODE>mode == SImode))
+    {
+      if (<ANYI:MODE>mode == HImode)
+        emit_insn (gen_byte_crc16_hi (operands[0], operands[1], operands[2]));
+      else
+        emit_insn (gen_byte_crc16_si (operands[0], operands[1], operands[2]));
+      DONE;
+    }
+
   /* If we have the ZBC or ZBKC extension (ie, clmul) and
      it is possible to store the quotient within a single variable
      (E.g.  CRC64's quotient may need 65 bits,
diff --git a/gcc/config/riscv/peephole.md b/gcc/config/riscv/peephole.md
index e49bc0426..f2e488b4e 100644
--- a/gcc/config/riscv/peephole.md
+++ b/gcc/config/riscv/peephole.md
@@ -19,6 +19,37 @@
 ;; <http://www.gnu.org/licenses/>.
 
 ;; Simplify (unsigned long)(unsigned int)a << const
+(define_insn "packed_field_mul"
+  [(set (match_operand:SI 0 "register_operand" "=r")
+	(unspec:SI [(match_operand:SI 1 "register_operand" "r")]
+		   UNSPEC_XBMUL))]
+  "TARGET_XBMUL && !TARGET_64BIT"
+  ".insn r 0x0b, 5, 0, %0, %1, x0"
+  [(set_attr "type" "imul")
+   (set_attr "mode" "SI")])
+
+(define_code_iterator packed_field_shiftrt [ashiftrt lshiftrt])
+
+(define_peephole2
+  [(set (match_operand:SI 0 "register_operand")
+	(packed_field_shiftrt:SI (match_operand:SI 1 "register_operand")
+		      (const_int 2)))
+   (set (match_operand:SI 3 "register_operand")
+	(packed_field_shiftrt:SI (match_dup 1) (const_int 5)))
+   (set (match_operand:SI 2 "register_operand")
+	(and:SI (match_dup 0) (const_int 15)))
+   (set (match_operand:SI 4 "register_operand")
+	(and:SI (match_dup 3) (const_int 127)))
+   (set (match_operand:SI 5 "register_operand")
+	(mult:SI (match_dup 2) (match_dup 4)))]
+  "TARGET_XBMUL && !TARGET_64BIT
+   && (rtx_equal_p (operands[2], operands[5])
+       || peep2_reg_dead_p (5, operands[2]))
+   && (rtx_equal_p (operands[4], operands[5])
+       || peep2_reg_dead_p (5, operands[4]))"
+  [(set (match_dup 5)
+	(unspec:SI [(match_dup 1)] UNSPEC_XBMUL))])
+
 (define_peephole2
   [(set (match_operand:DI 0 "register_operand")
 	(ashift:DI (match_operand:DI 1 "register_operand")
diff --git a/gcc/config/riscv/riscv.md b/gcc/config/riscv/riscv.md
index d2883ef9f..83d2c73b3 100644
--- a/gcc/config/riscv/riscv.md
+++ b/gcc/config/riscv/riscv.md
@@ -100,6 +100,10 @@
   UNSPEC_CRC
   UNSPEC_CRC_REV
 
+  ;; Packed bit-field multiply and byte-wise CRC16.
+  UNSPEC_XBMUL
+  UNSPEC_XCRCU8
+
   ;; Stack Smash Protector
   UNSPEC_SSP_SET
   UNSPEC_SSP_TEST
diff --git a/gcc/config/riscv/riscv.opt b/gcc/config/riscv/riscv.opt
index 2c26ee996..4225386ef 100644
--- a/gcc/config/riscv/riscv.opt
+++ b/gcc/config/riscv/riscv.opt
@@ -81,6 +81,14 @@ mdiv
 Target Mask(DIV)
 Use hardware instructions for integer division.
 
+mxbmul
+Target Mask(XBMUL)
+Recognize packed bit-field multiply expressions and use the custom xbmul instruction.
+
+mxcrcu8
+Target Var(riscv_xcrcu8) Init(0)
+Use the custom byte-wise CRC16 instruction.
+
 march=
 Target RejectNegative Joined Negative(march=) Var(riscv_arch_string) Save
 -march=	Generate code for given RISC-V ISA (e.g. RV64IM).  ISA strings must be
