# 280 MHz 时序继续迭代（2026-07-17）

- 日期：2026-07-17
- 状态：本轮完成；280 MHz 尚未满足时序，RTL 已恢复 v6
- 固定边界：顺序单发射，优先保持五级；PLL requested/actual 均为 280.000 MHz，VCO 1400 MHz，DIVCLK=1、feedback=7、CPU divider=5；完整主样例门槛为 476,000,000 cycles（1.70 s）和 490,000,000 cycles（1.75 s）。
- 输入基线：v6 为 460,174,656 cycles、380,344,384 instructions；正式 `Performance_Retiming` routed WNS/TNS/endpoints/WHS 为 -0.084 ns/-0.204 ns/8/+0.062 ns；固定 routed DCP 的一次 post-route `AggressiveExplore` 最佳为 -0.018 ns/-0.089 ns/7/+0.062 ns。

## EXP-016：EXU 寄存 late-load 完成结果

- 假设/预期收益：把 WBU/LSU late operand、32-bit ADD 和 IDU late forwarding 拆成两拍；允许少量固定周期回退，目标是消除 v6 的 `validReg_5 -> late ADD -> IDU operand` 组合路径，同时仍低于 476M cycles。
- 最小实现：EXU 内增加两个 valid 和两份 32-bit 完成数据寄存器，LSU source 优先；完成结果下一拍提供给 rs1/rs2 late forwarding。初版 capture 条件过宽，定向仿真暴露后改为 LSU-priority 条件。
- 功能门禁：`reformat`、Verilog、`add`、`load-store`、`switch`、`if-else`、`recursion`、`mul-01`、`mulh-01`、`mulhu-01`、`mulhsu-01` 均通过；lint 只有 7 个已知 `PINCONNECTEMPTY`。
- 性能：完整主样例正常结束，460,406,199 cycles、380,344,384 instructions、CPI 1.2105；仅比 v6 多 231,543 cycles，280 MHz 约 1.6443 s，满足继续门槛。原始目录为 `npc/history_perf/5aaafb90/0717T14_06_34.770116160/`。
- 正式实现：冷启动 `Performance_Retiming` 为 WNS/TNS/endpoints/WHS = -0.141/-2.048/44/+0.043 ns；top1 为 `payloadReg_5_isLoad/C -> payloadReg_4_info_reg2[24]/D`，13 levels，logic 1.195 ns、route 2.453 ns。synth/routed/report SHA-256 为 `bf928d1d385ed98d9153d7ec2e2574327c7acefeebc7cb676a323be228469cb1`、`c7d7849e26d46d39614690b84133d87907b1bd6613c90ee9a66025ded382d25d`、`131b7a908918d5e7bd1045a4388c101c561805c0e07a8ae748c5e68216e918cf`。正式 post-route `AggressiveExplore` 进程 SIGSEGV，不能作为结果。
- 固定同一 synth DCP 的有限比较：exact Auto_1（opt Explore、place Auto_1、physopt AggressiveExplore、route NoTimingRelaxation）为 -0.098/-1.569/34/+0.019 ns；其唯一一次 post-route `AggressiveExplore` 改善到 -0.060/-0.698/20/+0.019 ns。最终 DCP/report SHA-256 为 `376873a83557890dbf6f260f4e08170027a70e99d5e143c2baaf26e23616da7b`、`8a4a5875dab424a50726f463c834e29e3c52cc4422770d9ea587457ae56aef5b`。
- 结论：周期代价很小，但新增寄存器和选择器使违例端点扩散，最佳 -0.060 ns 仍差于 v6 的 -0.018 ns；未生成 bitstream，RTL 已回退。

## EXP-017：流水 payload 无条件采样协议检查

- 假设/预期收益：只让 valid 受 `allowIn` 控制，宽 payload 每拍采样，从而移除高扇出 CE cone。
- 验证：生成 RTL 证明 payload CE 被移除；同时加入“阻塞时 producer valid/bits 保持”断言。最基础的 `add` 仿真在首拍阻塞处触发 `blocked pipeline payload must remain stable`，具体为 IFU→IDU 边界。
- 结论：当前上游并不保证 ready=false 时 bits 稳定；统一无条件采样会改变被阻塞指令，属于协议不成立而非测试偶发。未进入综合，改动和断言已全部回退。

## EXP-018：v6 最佳物理解的定向物理修复

- 假设/预期收益：v6 最佳 DCP 仅差 18 ps，先对具体关键对象做确定性修复，不重新扫描全局策略。
- `phys_opt_design -critical_pin_opt`：从 -0.018/-0.089/7/+0.062 ns 开始，处理 0 cells/nets，结果完全不变。输出 DCP/report SHA-256 为 `7ff437c5c01b7e5ec91359afecd51eaab73a9e0dda61baf25bc635cd1aa64e34`、`c34846569db75d87fcddd9981208494b60b8c9e6fba30dbb80a34aa9ee9457b1`。
- top1 诊断：隐藏 IROM `RAMB36E1` 输出到 `payloadReg_3_code_reg[5]/D`，1 level（LUT3），logic 1.843 ns、route 1.414 ns，clock skew -0.312 ns；WNS -0.018 ns。第二条才是 EX/ID forwarding，为 -0.016 ns。
- 单网 ECO：只 unroute/reroute `irom/douta[5]`，使用 `route_design -delay -nets` 并 finalize；该网 route 从 1.414 ns 降至 1.413 ns，但总体仍为 -0.018/-0.088/7/+0.062 ns。DCP/report SHA-256 为 `2e374c0e1878d93f8ac9ca5cf892b2866ff8e339eed1c68a05c551dbc1f6d1a5`、`0b354721870c824ecc763119ded05b196652d3dd4ab0d5e9b1a8a2f3bd3d05bd`。
- 结论：当前 8～18 ps 违例分布在 IROM→IF/ID 和 EX/ID 多条独立路径；单点 ECO 不能形成可验收解，停止继续手工布线。

## EXP-019：IROM primitive output register + 两 credit fetch queue

- 日期 / 状态：2026-07-17～18；canonical cold implementation 已完成，主样例完整 RTL perf 由主代理并行执行中；本节的 Vivado 结果因大时序违例判为失败，不生成 bitstream。
- 负责人 / 边界：`vivado_280_irom` subagent 只负责归档旧生成物、XCI canonicalization、280 MHz cold implementation、独立 reopen 和结果记录；未修改 RTL。候选仍保持顺序单发射，目标是把 IROM primitive output register 合并为现有 IF/ID 边界，并用两 credit metadata/output queue 吸收固定两拍 ROM latency。
- RTL / dirty identity：仓库 `HEAD=5aaafb909485802bc572c4f00d7bbe16eff82e99`；候选相关 dirty diff SHA-256 为 `0573f3cf135456792a76d74cdb47822da982a69c8eba50042ae605704404d898`。implementation 启动前后关键源文件 SHA-256 均保持不变：`Top.scala=714354fee7bbd2ae512d53325d6246b60f602d5ba82cb17f5d7d75ab72bfd482`、`ifu.scala=f7676adb5af06deec66d8c141584102698ffe36d7237df1b3680f1f2206ca8d8`、`jydDevices.scala=581bc62cdb48fbabeb1533302fdfeb44544f80bf8b79911b6d4453681aed40a4`。
- cold / XCI 规范化：旧 `.Xil`、`.cache`、`.gen`、`.runs` 和 utils imported `top.dcp` 先移动到 `/tmp/jyd-vivado-irom280-cold-pre-20260717T235635`，未删除。第一次 cold open 把 IROM XCI 从 `0a083e27c7bdd2a5cc9ad0cada71d0c77afef31062a12651f408af883b4c7938` 规范化为 `1ffcc0e81e384c1a68ed706fea6b662725d82aea0bd71cabe40652b2e54a5b6f`；逐行审核确认唯一额外变化是 `Use_REGCEA_Pin=false` 条目的 GUI `enabled:false` 元数据被移除，关键语义仍为 `Register_PortA_Output_of_Memory_Primitives=true`、`READ_LATENCY_A=2`、`C_HAS_MEM_OUTPUT_REGS_A=1`、`C_READ_LATENCY_A=2`，生成仿真 wrapper 也为 `C_HAS_MEM_OUTPUT_REGS_A(1)` / `C_READ_LATENCY_A(2)`。该次 run 按流程立即作废并归档到 `/tmp/jyd-vivado-irom280-invalid-xci-normalize-20260717T235822`；随后以 `1ffcc0...` 为 canonical 输入重新 cold run，正式进程前后 XCI 逐字节不变。
- 工具 / 输入：Vivado `2024.2`，SW build `5239630`、IP build `5239520`；命令 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8`。PLL requested/actual 均为 `280.000000 MHz`，误差 `0.000000 MHz`，VCO `1400.000000 MHz`，DIVCLK `1`、feedback `7`、CPU output divider `5`。工程验证 24 个 `pack-fpga` 源均位于当前仓库内；`synth_1/top.tcl` 解析的 30 个 Verilog/SystemVerilog 源清单（路径及逐文件 SHA-256）总 SHA-256 为 `ab220bc0eb852ddc43be5fcea7999d8122e46d1d1d981e5a910da7b0a50216ed`。IROM/DRAM COE SHA-256 分别为 `0cea80f2ca36e2672ac8d1e3d0087f88dc24b5a33a177c74b47330b0637c6a1b`、`d1c6d8f4adbe80d618ccfccc0336a9a61b56007b0f44a4e79bddf71ccab89c03`；全部 project IP 在 run 前后均未 locked。
- 正式策略 / 实际 directives：run `synth_1` / `impl_1`，全量 `reset_run synth_1`，禁用 incremental checkpoint；实际命令依次为 `synth_design -top top -part xc7k325tffg900-2`、`opt_design`、`place_design -directive ExtraPostPlacementOpt`、`phys_opt_design -directive AlternateFlowWithRetiming`、`route_design -directive Explore`、`phys_opt_design -directive AggressiveExplore`（post-route）。没有 `Designutils 20-1307` 或 `Constraints 18-513` 硬错误；已有 `my.xdc` 两项无效 false-path critical warning 与此前工程一致。
- routed 时序：WNS/TNS/failing endpoints/WHS/THS = `-0.759 ns / -439.966 ns / 2350 / +0.071 ns / 0.000 ns`。top1 为 `pc_reg[3]/C -> ifu/meta0_pred_take_reg/D`，10 levels（`CARRY4=1 LUT3=1 LUT4=1 LUT6=5 MUXF7=1 MUXF8=1`），data delay `4.263 ns`，其中 logic `1.041 ns`、route `3.222 ns`。top2～4 从 `payloadReg_3_info_reg1_reg[11]/C` 到 `ifu/out0_code_reg[*]/CE`，9 levels，data delay 约 `3.968～3.973 ns`，logic `0.890 ns`、route `3.078～3.083 ns`；top5～10 同一 source 到 `out1_code_reg[*]/CE` 或 `out1_pc_reg[*]/CE`，9 levels，data delay `3.956 ns`，logic `0.890 ns`、route `3.066 ns`。
- post-route `AggressiveExplore`：WNS/TNS/failing endpoints/WHS/THS = `-0.666 ns / -430.328 ns / 2322 / +0.037 ns / 0.000 ns`。top1～3 从 divider `specialReg_reg/C` 到 `ifu/out0_code_reg[*]/CE`，7 levels，data delay `3.946～3.951 ns`，logic `0.524 ns`、route `3.422～3.427 ns`；top4～10 从 `ifu/out0_code_reg[4]/C` 到 `out1_code_reg[*]/CE` 或 `out1_pc_reg[*]/CE`，8 个 `LUT6`，data delay `3.926～3.942 ns`，logic `0.567 ns`、route `3.359～3.375 ns`。post-route pass 只改善 93 ps，违例端点仍超过 2300 个，不满足小时序违例的有限策略条件。
- 产物 SHA-256：`synth_1/top.dcp=1fdf82680b82ff0bd75349b904c79ba606dd1440f3d8438bb28d207f90b6533b`；`top_routed.dcp=d0615dd0786d4010e655eaa61e24ac96527ac1197ca47a35ddabe70aa59200fb`；`top_postroute_physopt.dcp=d0a5baffe8df0cc0109648a992abc077cdb6494f8914188f283f02b5c56c4447`；routed/post-route 工程 timing report 分别为 `7eaac1de82a05f5b6f5e9def90da687f36062fadb3c364d32aa08be3220b17e1`、`30a218689ad5b711e1c36e5002b8a10edd0e28971c85b891fe7f9e2e962912e8`。
- 独立 reopen：分别重新打开 `top_routed.dcp` 和 `top_postroute_physopt.dcp` 并执行 `report_timing_summary -delay_type min_max -max_paths 10 -report_unconstrained -check_timing_verbose`，重报数值与工程报告完全一致；`/tmp/irom280-routed-reopen.rpt`、`/tmp/irom280-postroute-reopen.rpt` SHA-256 分别为 `eb5fcaae7e6049053532bdc84a2b638121c6cab504daf8f9851c27c9ac02de20`、`b0fcc94f5b7ced72b8d12931575a10aafbda939f8a7c973ad60ea7e5276421d4`。
- 结论 / 原因：IROM primitive output register 确实移除了旧的 BRAM-Q→IF/ID 数据路径，但两份 metadata/output queue 的共享 credit、flush、backpressure 和 store/dequeue 条件把 PC、ID/EX payload、divider 状态等远端寄存器接入宽 payload CE，形成 7～10 层、route 主导的全局控制锥；时序从 v6 最佳 `-0.018 ns` 扩散为数千端点、`-0.666 ns`。该结构不能靠有限 implementation 策略关闭，Vivado 方向立即止损；若性能仿真本身通过，也只能据此重新设计局部 ready/valid 与 queue write-enable，不能保留当前共享控制锥进入 bitstream。

## 本轮结论与下一方向

- 280 MHz 仍未达到 `WNS >= 0`，本轮没有生成或上板任何负 WNS bitstream。
- 生成物已重新由 v6 RTL产生，`reformat`、`checkformat`、Verilog 均通过；EXP-016/017 的 CPU RTL 不留在工作树。
- 下一轮应把 IFU/IROM 边界作为与 late forwarding 同等优先级的结构问题：研究仅对 instruction response 做稳定 skid/预取寄存，或让 IROM 输出寄存与现有 IF 级合并，必须先证明不增加第六个可见流水级，并用早期主样例轨迹约束周期代价。不要继续增加 EX forwarding mux，也不要重复全局 implementation directive。

## EXP-020：v6 operand 源网定向强制复制

- 日期 / 状态：2026-07-18；唯一候选完成，时序退化，停止该物理方向；未修改 RTL/XCI，未生成 bitstream。
- 输入身份：候选四文件已回到 v6；`ifu.scala`、`Top.scala`、`jydDevices.scala` SHA-256 分别为 `2343de527ddaf036199bdfe277567cac67a4966327930c724736cfa91d76dba0`、`1b44f3f3d87f2fb70cd9cc0d65677d28c8cca2d40a5b21ef7a420ab1e17d901c`、`72e77a336208dd6d3d2ed116fda16be8e9fae7248961f17532e32e619ec6b415`；IROM XCI 仅相对仓库版本增加 EOF newline，语义仍为 primitive output register `false`、`READ_LATENCY_A=1`，SHA-256 `ebb29951e158c59a6e4b5904d65fe87b464b1612d2d32cee226d94fef4a6039a`。`mypll.xci` SHA-256 为 `706ce149807ac55ba3e765ae076b4b29b21d3f0a6e5155fd9eb80691d908c7ae`；requested/actual 均为 `280.000 MHz`，MMCM DIVCLK=1、feedback=7、CPU divider=5，VCO 1400 MHz。
- 固定 DCP 血统：沿用 `v6-audit-frozen-A` 的唯一 synthesis，`synth_1/top.dcp` SHA-256 `8fc99e2c3e330ac869b0f00cae7b9c5ee29abebdc6a347d2cda61c82f7804332`；正式 `top_physopt.dcp`（已完成 `place ExtraPostPlacementOpt` 和 `phys_opt AlternateFlowWithRetiming`）SHA-256 `91e116c53dd0ad6efc75ece0db666032fd6c0a93f907b8a37ed3359af97884ad`。用于提出假设的同血统最佳 post-route DCP SHA-256 为 `ea9399c7a890fa60cac598b7319d72af95723a25108e0acabf5c3c7179cc61eb`，基线 `-0.018/-0.089/7/+0.062 ns`。
- 路径依据 / 假设：基线 top1 IROM `douta[5]` 仅 fanout 2，且 EXP-018 单网 ECO 无效；第二及后续最差路径从 `payloadReg_4_info_reg1_reg[1]/Q` 发出，该网报告 fanout 34、路径 route 占 69.1%。假设只复制这一条 operand/forward source net，可缩短 EX/ID forwarding 路由而不扰动 RTL。
- 实际 Tcl / directives：从 `top_physopt.dcp` 打开后执行 `phys_opt_design -force_replication_on_nets [get_nets {student_top_inst/mytop/cpu/payloadReg_4_info_reg1[1]}]`；实际对象 fanout 为 30，Vivado 创建 4 个 replica。随后只执行原正式 `route_design -directive Explore` 和原最佳 `phys_opt_design -directive AggressiveExplore`。没有开放策略扫描，也没有处理第二条 net。首次同进程 post-route physopt 无 Tcl error 异常退出；从已固定 routed DCP 重启完全相同的 post-route 命令后正常完成。
- routed 结果：WNS/TNS/WHS/THS = `-0.209/-12.082/+0.077/0.000 ns`，明显差于未复制的同血统 routed 结果；routed DCP SHA-256 `2d77d307bb7c3f9c76a98be7569ecacb0bc24b3cc6f51add05de01f4c2d1791e`。
- post-route 结果：WNS/TNS/failing endpoints/WHS/THS = `-0.109/-4.555/90/+0.077/0.000 ns`。top1 为 `payloadReg_3_code_reg[3]/C -> ifu/pcReg_reg[10]/CE`，7 levels（LUT4=1、LUT6=6），data delay `3.303 ns`，logic `0.524 ns`、route `2.779 ns`（84.1%），clock skew `-0.122 ns`；top2～10 是同一 source 到 `pcReg[10/14/15]/CE` 的同类路径。复制虽移走了目标 operand 网的原 top path，却扰动布局/布线并暴露更差的 IFU PC-enable 控制锥。
- 产物：目录 `/tmp/jyd-v6-targeted-replication-20260718/`。pre-route/routed/post-route DCP SHA-256 分别为 `9c83de225d4fd5b00ffd894bb22950fe4c1c4b224c5645001bc843e22515095d`、`2d77d307bb7c3f9c76a98be7569ecacb0bc24b3cc6f51add05de01f4c2d1791e`、`71967423eff09a26e853cdd3ba1cf393220551c13d80038629ef3afd2f55df39`；timing summary/top10 setup/top10 hold/route status SHA-256 分别为 `2a647776fcf9233174a045682ad22d7969b8a3cfdf3fd726033b837f45221d39`、`d3f4780771a4c2ae7d058a9a010f3b11fd78971eda5c0df4ecf3fabb8385b60c`、`b4c5f6897664ec206d9c76864cc79313dbdcae23476f6b8d6ec6ddfc6a3a129e`、`00202eae881da36d82acfc5bc74317a040f423dc25a6638cc90d548e992f472a`。
- 结论：单条 operand 源网的强制复制不是可验收修复；它对几十皮秒级、多族路径的 v6 解扰动过大，最终比 `-0.018 ns` 退化 91 ps。按“至多一个机制明确的 targeted optimization”边界停止，不继续复制其他 bit/net。下一步应回到 RTL 结构，优先缩短 IFU PC/enable 控制锥或局部化 forwarding，而不是继续 post-route ECO。

## EXP-021：ID/EX addReg1/addReg2 局部 operand 副本

- 日期 / 状态：2026-07-18；完整功能和性能门禁通过，canonical cold 280 MHz implementation 完成，但时序明显退化，结构方向失败；未生成 bitstream，候选 RTL 已由主代理回退。
- 假设 / 最小实现：在 ID/EX payload 中增加 `addReg1/addReg2` 两份局部 operand 副本，并让 EXU 的 ADD 输入优先使用副本，期望降低原 `reg1/reg2` 对 forwarding、地址/分支和 ADD 的共享扇出；仍为顺序单发射五级。候选只修改 `common_def.scala`、`exu.scala`、`idu.scala`，启动时三文件 SHA-256 分别为 `f6480d64d18f8c60a4ec7bf5952798776089634576f3a7c2b45720b64133f15a`、`eb9abf3ea9b9304245e8039a9f8d472b8eb7c305fe9fc16cbc6c667b42e8d5c0`、`2ab07c7e5db7a04ff30734446aa4491ef1b62109d66d2bc74213574877ffad88`；三文件 dirty diff SHA-256 为 `a5e0c6a947ea9a439ddbfdf5e602bf7413fa2a42ced564236cb9a14ad2ff9726`。
- 并行启动 / 最终性能：主样例在 16M、32M、48M instructions 检查点的 cycle/CPI 轨迹均与 v6 完全一致，因此按 4.1 节提前由 `add_operand_vivado` subagent 启动 implementation。完整 RTL perf 后续正常结束，真实结果为 `460,174,656 cycles`、`380,344,384 instructions`、CPI 约 `1.2104`，与 v6 完全相同；最终候选 diff hash 与启动身份一致。局部副本没有周期收益，只用于检验时序结构假设。
- cold / XCI canonicalization：旧 `.Xil`、`.cache`、`.gen`、`.runs` 先移动到 `/tmp/jyd-vivado-20260718T_add_operand280_pre`，未删除。首轮 cold open 的全部 XCI manifest 从 `34cb4209eaac9224baca7a5dfe8c98bcb5ae35ef7dd85ac30ab1e27e955b0536` 变为 `2e3b42a47bdbacce5dfb95a11c3ecfcac8750c8576f4cd3ff587bf2d12745ef5`；逐行审核仅为 `blk_mem_gen_2KB` 和 `blk_mem_gen_dram` 的 `PRIM_type_to_Implement=BRAM` 项移除 GUI `enabled:false` 元数据，关键 IP 参数不变。该轮按流程作废并归档到 `/tmp/jyd-vivado-20260718T_add_operand280_invalid_xci_normalize`。以规范化 XCI 重新 cold run 后，run 前后逐文件 SHA-256 一致；其中 2KB/DRAM/IROM/PLL 分别为 `4a84218e...`、`20e8110f...`、`fd83334c...`、`706ce149...`，全部 IP unlocked。
- 固定输入 / 工具：`HEAD=5aaafb909485802bc572c4f00d7bbe16eff82e99`，Vivado `2024.2`（SW `5239630`、IP `5239520`）；`pack-fpga.zip` SHA-256 `4c6501dd44c527e5dff3713931fa7b16119ed613014c6d8061de100cd345df64`。`synth_1/top.tcl` 解析的 30 个 Verilog/SystemVerilog 源清单及逐文件哈希总 SHA-256 为 `8abc3b7ad6d848900151b5b7ff6b5d7e2c8fba9977ffd828fed3a23ce87eca12`，24 个 pack-fpga 源均位于当前仓库。PLL requested/actual 均为 `280.000000 MHz`，VCO `1400 MHz`，DIVCLK=`1`、feedback=`7`、CPU divider=`5`；没有 `Designutils 20-1307` 或 `Constraints 18-513`。
- 正式 directives：`synth_design -top top -part xc7k325tffg900-2`、`opt_design`、`place_design -directive ExtraPostPlacementOpt`、`phys_opt_design -directive AlternateFlowWithRetiming`、`route_design -directive Explore`，随后唯一一次 `phys_opt_design -directive AggressiveExplore`。正式 post-route 进程 SIGSEGV；从已固定正式 routed DCP 只恢复同一个 `AggressiveExplore` 一次并成功，没有运行其他策略或重综合。
- routed 结果：WNS/TNS/failing endpoints/WHS/THS = `-0.217 ns / -8.664 ns / 130 / +0.065 ns / 0.000 ns`。top1 为隐藏 peripheral read register 到 `payloadReg_4_info_reg2_reg[17]/D` 的 late-load ADD/forwarding 路径，11 levels（`CARRY4=5 LUT5=2 LUT6=4`），data delay `3.665 ns`，logic `1.079 ns`、route `2.586 ns`；新增副本本身也出现在 top3/4/9，分别到 `addReg1[14]/[9]/[4]`，8～10 levels 且 route 占 `72.9%～75.7%`。
- 恢复后的 post-route 结果：WNS/TNS/failing endpoints/WHS/THS = `-0.116 ns / -6.163 ns / 100 / +0.065 ns / 0.000 ns`。top1 为 `payloadReg_4_info_reg2_reg[0]/C -> redirectTargetReg[2]/D`，9 levels，data delay `3.535 ns`（logic `0.892 ns`、route `2.643 ns`，route 74.8%），clock skew `-0.133 ns`；top2～9 主要为 `payloadReg_4_code_reg[14]/C -> payloadReg_3_code[*]/CE`，7 levels（`LUT4=1 LUT6=6`），data delay `3.306 ns`，route `2.782 ns`（84.2%）。副本没有关闭原 forwarding/redirect 控制族，反而增加新的 ID/EX data endpoints。
- 产物 SHA-256：`synth_1/top.dcp=3ca904c57c90ff1a01e6903db660b6ff666ca7bf550ea9c1d33848551b998349`；`top_routed.dcp=de769a77f02651c21ca5b89c5e4635c91434907937c7801e7be54914419bd991`；routed report `d36e0469f956870086bdcd7be0237c38b7c039a5fd097c5aac6939ad4b025ee8`；恢复后的 post-route DCP/report 分别为 `1ccfe8bb79d67bf6736a6f048b3279634c8e1f6d6d2034889a8a1a811a3d46cf`、`f86dd908b6753d5d9ec997ae600f5ac3f90010318d98b059a4653105b89a63dc`。
- 结论：该候选性能完全不变，但最佳 WNS `-0.116 ns` 比 v6 同血统最佳 `-0.018 ns` 差 98 ps，且违例从原少数 IROM/forwarding 路径扩散到 100 个 endpoint。局部复制宽 operand 增加了 ID/EX 寄存器及写入路由，却没有切断 late forwarding、redirect 和 pipeline CE 控制锥；属于结构性退化，不生成 bitstream、不做候选特定策略扫描。

## EXP-022：用 IROM `ena` 保持输出并删除 IFU skid mux

- 日期 / 状态：2026-07-18；RTL 功能与完整性能门禁通过，等待 280 MHz implementation 判定。
- 路径依据 / 假设：v6 最佳解的 top1 是 IROM BRAM `douta[5]` 经唯一 LUT3 到 IF/ID code 寄存器，WNS `-0.018 ns`。该 LUT 来自 IFU 的 `RegEnableReadNew(memIO.rdata, memIO.resp_valid)`：响应周期旁路 BRAM 数据，非响应周期选择本地保持寄存器。假设仅在真实读请求时拉高 IROM BRAM `ena`，使 BRAM `douta` 在 IFU backpressure 时自行保持，便可让 IFU 直接消费 `memIO.rdata`，去掉该 LUT 而不增加寄存器、队列、流水级或周期。
- 最小实现 / 协议核对：`ifu.scala` 将 `io.out.bits.code` 直接连接 `memIO.rdata`；`SimpleBusFPGAROM` 将 `mem.io.ena` 从常高改为 `doRead`。`SimpleBusROM` 的 inline 仿真模型原本已经使用 `doRead` 作为 enable，故仿真/FPGA 行为现在一致。IFU 进入 `waitOut` 时不能接受新 PC 或发新请求，BRAM 输出因此保持；响应同拍被消费并接受下一请求时，旧输出在时钟沿前供当前 transfer，新地址在沿后更新输出并对应下一拍 `resp_valid`，不改变 Decoupled 交叠语义。
- 快速门禁：`reformat`、`checkformat`、Verilog 生成均通过；lint 只有 7 个已知 `PINCONNECTEMPTY`。`add`、`load-store`、`switch`、`if-else`、`recursion` 均通过；RT-Thread 到达 `msh />` 后手动停止；架构测试仓库实际映射的 `mul-00`、`mulh-00`、`mulhu-00`、`mulhsu-00` 均 PASS。
- 性能：完整 `withMext-v2` RTL perf 正常结束，`460,174,656 cycles`、`380,344,384 instructions`、IPC `0.8265`、CPI `1.2099`，与 v6 基线总周期和指令数完全一致。原始目录为 `npc/history_perf/5aaafb90/0717T16_38_04.690781606/`。稳定检查点包括 16M/32M/48M/64M instructions 对应 `19,856,490 / 38,879,616 / 58,416,431 / 77,747,763 cycles`。
- 继续条件：性能与协议门禁均满足；该候选是否保留只取决于固定输入的 280 MHz implementation 能否移除旧 IROM LUT 路径且不恶化其他路径。subagent 本轮未启动 Vivado。

## EXP-023：IROM 直接消费 BRAM 输出并按请求门控 ENA

- 日期 / 状态：2026-07-18；canonical cold 280 MHz implementation 完成，时序明显退化，方向失败；未生成 bitstream，也未继续扫描策略。完整主样例 perf 与 M 扩展回归由主代理并行完成，本项只负责冻结候选与正式 Vivado 实现。
- 假设 / 最小实现：删除 IFU 对 `memIO.rdata` 的额外 `RegEnableReadNew`，直接把一周期 BRAM 输出作为现有 IF/ID payload；同时把 `SimpleBusFPGAROM.mem.ena` 从常开改为 `doRead`，希望消除 v6 的 IROM BRAM-Q 到 IF/ID 额外寄存路径而不新增可见流水级。候选仍为顺序单发射五级，仅修改 `ifu.scala` 与 `jydDevices.scala`。
- 并行启动依据：主样例在 64,000,000 instructions 时为 77,747,763 cycles，多个检查点的 cycle/CPI 轨迹逐点等于 v6，因此按流程 4.1 节提前启动 implementation；该外推只用于调度，不替代完整性能结果。
- 冻结输入：`HEAD=5aaafb909485802bc572c4f00d7bbe16eff82e99`；启动前后 `ifu.scala=53639c74287f66e6712096895dfc440876027145401eae0659694964ec29dbed`、`jydDevices.scala=1c5ae5eef654fd65f22b692b9d3779cd7d03c25c2f6397af9e4c7cad6db7245c`，两文件 dirty diff SHA-256 `b082abe506380666f3efd7e0c4273669a3ef11d0e3ab8223a31d441972dde483`；`pack-fpga.zip=4ef95618c5b0a188ef5933de02e8698dbe3a97aa96a9e8ee9cf7506b339f8af8`，生成目录逐文件 manifest SHA-256 `ac07dea546c2abd691067228a5532d876089c2540e2c1c50c8ce1b357299d1a9`。正式命令使用 `--skip-pack` 消费该冻结生成物，工程 imported 目录与其 `diff -qr` 完全一致；输入镜像 `withMext-v2.bin=b998eae7b2e6c3dd5911bc67927cea4e9c4f8a1bcb639391c520ba3391742749`。
- cold / XCI / IP 门禁：旧 `.Xil`、`.cache`、`.gen`、`.runs` 整体移动至 `/tmp/jyd-irom-hold-pre-cold-20260718/`，未删除。正式进程前后全部 XCI manifest 均为 `82f5aaa7dbd9acb13a91f3793f075be8c8253a3c9e692aa2b9de0a5d0f77bec6`，没有 XCI normalization；IROM XCI SHA-256 `fd83334cad22cd1c2879818c5ab9ac87699adcfac4dcdee57283cc42f0b2aced`，语义为 primitive output register `false`、`READ_LATENCY_A=1`、`C_HAS_MEM_OUTPUT_REGS_A=0`。全部 8 个 project IP 在 run 前和重新生成后均 unlocked；约束 manifest SHA-256 `4a23ad97e6900cb1cd2acd23f0af0023040003a40f9ab5315f354cd66f93b5a4`，COE manifest SHA-256 `410e203a5bef99d4964b38997afa7a015f5782739a1b61acfca74ed701de5d1a`。
- 工具 / 源路径 / 时钟：Vivado `2024.2`，SW build `5239630`、IP build `5239520`；命令 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8 --skip-pack`。工程实际解析 24 个 `pack-fpga` 源，全部位于当前仓库内；`synth_1/top.tcl` SHA-256 `399719a7c676b2191318f53b2fe0d20c081773f5d64519dd96f5daa46cceb4a6`，其 `read_verilog` 路径审核未越界。PLL requested/actual 均为 `280.000000 MHz`，误差 0，VCO `1400 MHz`，DIVCLK=1、feedback=7、CPU divider=5；综合传播周期为 `3.571 ns`，没有 `Designutils 20-1307` 或 `Constraints 18-513` 硬错误。
- 正式 directives：全量 `reset_run synth_1`、禁用 incremental checkpoint；实际命令依次为 `synth_design -top top -part xc7k325tffg900-2`、`opt_design`、`place_design -directive ExtraPostPlacementOpt`、`phys_opt_design -directive AlternateFlowWithRetiming`、`route_design -directive Explore`、唯一一次 post-route `phys_opt_design -directive AggressiveExplore`。
- routed 结果：WNS/TNS/failing endpoints/WHS/THS = `-0.116 ns / -3.853 ns / 89 / +0.090 ns / 0.000 ns`。top1 为隐藏 divider result FDRE 到 `payloadReg_4_info_reg2_reg[13]/D` 的 late-result/forwarding 路径，11 levels（`CARRY4=4 LUT1=1 LUT5=1 LUT6=5`），data delay `3.594 ns`，logic `1.109 ns`、route `2.485 ns`（69.1%），clock skew `-0.105 ns`。该候选移除了旧 IROM top path，但没有缩短现有 late forwarding 数据锥。
- post-route `AggressiveExplore`：WNS/TNS/failing endpoints/WHS/THS = `-0.109 ns / -3.820 ns / 89 / +0.090 ns / 0.000 ns`，仅改善 7 ps。top1 改为隐藏 peripheral read FDRE 到 `payloadReg_4_info_reg2_reg[19]/D`，9 levels（`CARRY4=3 LUT5=2 LUT6=4`），data delay `3.524 ns`，logic `1.063 ns`、route `2.461 ns`（69.8%），clock skew `-0.137 ns`；后续 top paths 仍主要落在 late load/add forwarding 到 `reg1/reg2` 以及相关控制端点。
- 产物 SHA-256：`synth_1/top.dcp=1bd51657394f041b7071e455d6eabbba40d4051920d6c82a2805468dae7c9b4a`；`top_routed.dcp=3baa42a7234eeefea933c8701df24607847efe9cce938ed407a6dc6536bbed5b`；`top_postroute_physopt.dcp=6c3c4b1d0d5a3fa9e8cac9ba33330861ab572edfd46edd96017ab3abe3df7923`；routed/post-route timing report 分别为 `9a5b5bbdc32c5565fb4fdde6444ffe75732ad9cd0e7ecc28b2dbe288992cf893`、`a8706ab3d3ab4bf58cf2ff82bfedd1edb3ea906d720668370a0c193110df9716`。
- 独立 reopen：分别重新打开 routed 与 post-route DCP，使用 `report_timing_summary -delay_type min_max -max_paths 10 -report_unconstrained -check_timing_verbose` 重报，数值与工程报告完全一致；`/tmp/irom-hold-280-routed-reopen.rpt`、`/tmp/irom-hold-280-postroute-reopen.rpt` SHA-256 分别为 `62744aba66d8071a80d5e0d18e29856d5d18a2ae50d75ee7d47b429f9e8d8f84`、`338c38fd0195e1c0a334ab3a048dad75c82886942e7fd3015a3fec5ba91ba40b`。
- 结论 / 下一步：直接消费 BRAM 输出达成了移除 IROM→IF/ID 关键路径的局部目标，但总体 WNS 被原有 late-load/late-add forwarding 数据锥接管，正式唯一 post-route pass 后仍为 `-0.109 ns`，比 v6 同血统最佳 `-0.018 ns` 差 91 ps，且超过流程中的小时序违例探索边界。该结构不生成 bitstream、不做额外策略；后续应针对 late-result 到 ID/EX operand 的架构边界做局部化，而不是继续调整 IROM 或随机布线。
- 候选特定有限策略复查：按流程 6.2 节严格复用上述 canonical `synth_1/top.dcp`（SHA-256 `1bd51657394f041b7071e455d6eabbba40d4051920d6c82a2805468dae7c9b4a`），没有重新 pack 或综合；冻结副本和 standalone 产物位于 `/tmp/jyd-exp022-auto1-20260718-v3/`。实际命令只有 `opt_design -directive Explore`、`place_design -directive Auto_1`、`phys_opt_design -directive AggressiveExplore`、`route_design -directive NoTimingRelaxation`，以及同一次唯一 post-route `phys_opt_design -directive AggressiveExplore`。Vivado 明确报告 7-series 不支持 Auto，实际把 `place_design Auto_1` 降级为 `Explore`。首次两次隔离启动分别因只复制 XCI、复制 IP 目录后 XCI output-product 路径仍未解析而在 `opt_design` 前以 black-box DRC 停止，均未执行实现策略；最终按已有 standalone 流程引用 canonical IP output products，run 前后 XCI manifest 完全相同（manifest 文件 SHA-256 均为 `b71d2bd2bc9d9306b74ffcc25bd1ae410a59c4b302a42d97c63711c331604cc2`），没有修改 RTL/XCI 或覆盖正式 `impl_1`。
- Auto_1 routed / post-route 结果均为 WNS/TNS/failing endpoints/WHS/THS = `-0.132 ns / -3.270 ns / 71 / +0.090 ns / 0.000 ns`；8984 个 routable nets 全部 fully routed、0 route errors。唯一 post-route pass 优化 0 cells/nets，WNS/TNS 完全不变。top1～10 都是同一 `payloadReg_4_info_reg2_reg[3]/C -> payloadReg_4_info_reg2_reg[30]/D` late-result/forwarding 路径（不同边沿组合），15 levels（`CARRY4=8 LUT2=1 LUT4=1 LUT5=1 LUT6=4`），data delay `3.668 ns`，logic `1.261 ns`、route `2.407 ns`（65.6%），clock skew `-0.047 ns`。routed/post-route DCP SHA-256 分别为 `175dec032e4daede7e8429c533e40d74e2b72fb4e283868430b56667e2e92e05`、`b7c9e2677b123bdff7fc1d699f21ad277943999053572f8031e8bfbc354266fc`；对应 timing summary SHA-256 为 `00441d6564fba7f95e74211c0a0156696499e99b76fa239a5bedb7a9e3c76c30`、`6d7d50629764ad7fcb0dff962ff5b041189857a330134d7b59fd09c9af5988ed`，top10 setup report 为 `a2f71d6bb3a77dd7300144d02760e4abad3ef38d0f890c25eeef792719fa327d`、`fbd3a0d3ac8244846e20e0a9855011e3dc5a9c6311f3b02cf69e5a44fe008d2e`，Tcl/log 为 `52c130688d3a1d4226a1b9d8d43e1b09b9b66e34e2e4171a8f9649f4c7dfcb5e`、`eb15a3e304b0c71a009c98a7cb543fd48c459d8dc8f6e70eed7772fa6b7ae074`。
- 有限比较结论：exact Auto_1 最佳仍为 `-0.132 ns`，比同 synth DCP 正式 Retiming 的 `-0.109 ns` 更差 23 ps，且 post-route 没有任何可优化对象。候选特定的唯一预选策略仍为负，立即止损，不再扫描 directive、不生成 bitstream；该结果进一步确认剩余瓶颈是 15-level late-result/forwarding 结构而非 IROM 路由。
