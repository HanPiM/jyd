# v6 270 MHz 时序收敛记录

实验编号 / 标题：EXP-270-CLOSE-001 / v6 精确 270 MHz 正式 post-route 收敛  
日期 / 状态：2026-07-17 / 最终验收通过  
负责人或代理 / 任务边界：Codex / 不修改 CPU RTL；保持单发射顺序五级流水线，仅调整 PLL 与正式 implementation 流程  
假设和数据依据：v6 在 280 MHz 为 WNS -0.084 ns；既有 270 MHz routed 结果为 WNS -0.022 ns，最差两条路径 84% 为布线延迟，适合一次预先限定的 post-route physical optimization。  
预期收益与继续阈值：WNS 必须非负；按 v6 的 460,174,656 cycles 计算，270 MHz 为 1.7043506 s，低于 1.75 s 硬门槛并接近 1.70 s。  
RTL commit / dirty diff hash：`5aaafb909485802bc572c4f00d7bbe16eff82e99` / 本实验相关 diff SHA-256 `4d59ccf5137113b9170e4cd694c8289cd0535f9849646824df873607b61ad4fa`  
Vivado commit / IP 配置 / implementation 策略：工程内 `digital_twin.xpr`；PLL requested/actual 270.000000 MHz，VCO 1350 MHz，DIVCLK=4、feedback=27、CPU divider=5；`Performance_Retiming` 加正式 post-route `AggressiveExplore`。两个 BRAM XCI 仅接受 Vivado 2024.2 对失效 `PRIM_type_to_Implement.enabled` 字段的规范化删除，存储器配置未改变。  
Vivado 完整版本 / run 名 / 各步骤实际 directive / 是否 reset：Vivado 2024.2 build 5239630、IP build 5239520；`impl_1`；默认 `opt_design`、`place_design ExtraPostPlacementOpt`、`phys_opt_design AlternateFlowWithRetiming`、`route_design Explore`、post-route `phys_opt_design AggressiveExplore`；移走旧 `.gen/.runs/.cache/.Xil/ip_user_files` 后冷启动，脚本再次 reset synth、重生成全部 IP/OOC，incremental 禁用。  
解析后的综合源清单 SHA-256：`npc/build/pack-fpga` 逐文件清单 `3e10c7471849f5f6f3cdf7c4134784c21c546b148c918aa2f32ecfdfc7ba3ce1`；Vivado 门禁确认 24 个源均位于工程内 `imports/pack-fpga`。  
synth DCP / routed DCP / timing report SHA-256：`302a8c8818fb7949691a924d6237ec08b3397820a6f6402acc324770306328e7` / `35331ef9d3a1bc67e63b13d7deb3cf91b5a4cdb72c4bfd0728f5e21e84ee3558` / `a82fff0663751180dd75877e4bb3bb8d858a202ca603a8dee4c2fdb8af441db2`；post-route DCP `593016c048828798cbaed998eefba74b64adfb0304a88bd94695d6d3944a933e`。  
输入镜像或 COE SHA-256：`jyd-coes/withMext-v2` 逐文件清单 `556d5694747df229ad772d49b159c3b71f2371409740fad141db9fc0f0251023`。  
改动摘要：CPU RTL 不变；Clock Wizard 固定到精确 270 MHz；`impl_1` 和标准脚本均启用 post-route `AggressiveExplore`，确保报告、DCP 与 bitstream 属于同一正式 run。  
快速功能测试：`make -C npc pack-fpga` 通过；`verilog-lint` 仅有 AGENTS.md 允许忽略的 7 个 `PINCONNECTEMPTY`；`CCACHE_DIR=/tmp/jyd-ccache make -C am-kernels/tests/cpu-tests run ARCH=riscv32-jyd ALL=add` 通过（HIT GOOD TRAP）。  
代表性仿真：CPU RTL 与 v6 完全相同，沿用完整 `withMext-v2` 结果 460,174,656 cycles / 380,344,384 instructions，CPI 1.2099；本轮未重复完整仿真。  
时序：270.000000 MHz；route 后 -0.022 ns / -0.036 ns / WHS +0.021 ns，2 个 setup endpoints；正式 post-route 后 WNS +0.004 ns、TNS 0、WHS +0.021 ns、0 failing endpoints。8978/8978 routable nets fully routed、0 routing errors。  
bitstream SHA-256：`7c0f6cf558909165ab961e7020eacd1043bf8a2b1a1d13f943df2eaf9bf01952`（`jyd-vivado-proj/digital_twin.runs/impl_1/top.bit`）  
上板有效结果及中位数：固定同一 bitstream 三次结果均为 `37801704`（1.704 s），LED 均为 `0x078b7323`，客户端三次 `task_success=true`；中位数 1.704 s，最慢 1.704 s，满足每次不超过 1.75 s 和约 1.70 s 的目标。第 1 次使用 FPGA1，第 2、3 次使用 FPGA2，均正常烧录、稳定采样并释放板卡。  
基础设施失败和重试：首次正式 pack 被损坏的 Mill `out` 缓存触发 `ClassNotFoundException`，移到 `/tmp` 后重建通过；首次共享工程启动被旧 280 MHz `mypll.xml` 锁定门禁拒绝，移走生成目录后冷重建；首轮生成发现两个 BRAM XCI schema 规范化，固定规范化文本后第二次冷 bitstream 全流程 XCI 前后哈希一致并成功退出。  
结论：最终保留并通过验收。270 MHz 正式流程实现非负 WNS，三次上板功能与性能一致，无需进入高风险 late-load/六级结构改动。  
依据与下一步：候选同时满足功能、WNS >= 0、三次有效上板均 <= 1.75 s、中位数约 1.70 s。后续提交时保持本记录、PLL XCI、`digital_twin.xpr` 与标准 Vivado 脚本为同一逻辑变更，并记录上述 bitstream 哈希。
