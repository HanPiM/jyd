# 1.70 s / 280 MHz 新方向迭代

## EXP-024：新方向动态计数基线

- 负责人：`newdir_perf_counters` subagent；仅修改仿真侧
  `npc/sim/PerfCounter.{hpp,cpp}` 和 `npc/sim/PerfFmt.cpp`，没有增加 Chisel
  信号，计数逻辑不进入 `pack-fpga`。
- 输入身份：v6 CPU 数据通路加 EXP-025 BTB 双 LUTRAM 候选，而不是纯 v6。
  `BranchTargetBuffer.scala` SHA-256 为
  `152492ccf91d4968ba7a2503fbdb014f1a3c8e24b444189c0154c418e03168a0`；
  BTB 候选已单独证明周期完全不变。
- 固定镜像：`jyd-tests/2026/bin/withMext-v2.bin`，SHA-256
  `b998eae7b2e6c3dd5911bc67927cea4e9c4f8a1bcb639391c520ba3391742749`。
- 完整 RTL perf 正常结束：460,174,656 cycles、380,344,384
  instructions、CPI 1.2099；64M/128M 检查点分别为 77,747,763 和
  155,309,184 cycles，均与 v6 精确一致。280 MHz 理论时间为
  1.643481 s。
- M 扩展发射次数：`mul=10,496,004`、`mulh=128,004`、`mulhsu=4`、
  `mulhu=18`、`div=4`、`divu=4`、`rem=4`、`remu=4`；div/rem 合计仅
  16 次。这支持为普通 `mul` 建立低 32 位快速 IP，但 divider
  降资源不能期待可测性能收益。
- cacheable full-word store 为 21,450,238 次。该数量与历史上撤掉
  full-word write-allocate 后约 21.09M 周期退化同量级，说明此性能交换
  风险很高，必须依赖快速 `mul` 的约 10.50M 周期收益后再单独筛选。
- late-load ADD/ADDI 选择：rs1-only 57,668,883、rs2-only 5,312,000、
  both 0；late-add 后继选择：rs1-only 26,176,000、rs2-only
  20,868,862、both 0。rs2 late-add 并非冷路径，静态删除该通道会产生
  大量额外 stall，与 v14 的明显性能退化一致，不作为首选方向。
- 验证：JYD 仿真器重新编译通过；`reformat`、`checkformat`、`verilog`
  通过；`git diff --check` 通过。原始报告目录为
  `npc/history_perf/5aaafb90/0717T17_41_11.195354100/`，raw JSON/report
  SHA-256 分别为
  `9a0ffc98328fd95f7d3b14d0102c48d627342d4d7e9e1c344d96c0ba8622fce1`、
  `6343c7d2e1d1d4cc0df12f4818190fe10bfc13abc60bfedb13d1f7e058a08656`。
- 三个计数源码 SHA-256 依次为
  `6b6b1fe4109839c18ede3635801e82ed46287f5ed7c4ef612870e072e8756855`、
  `23ffb66b805bb155b7efee877640bb4851555f562af4cddd0c947e9ef051b3e6`、
  `f26c3e17c0fa2ffcc5bc3927037dff4d0aceb4905475adf4044053ffff2d15fb`；
  三文件 diff SHA-256 为
  `63a17a44df36e1ed9161fce14fe655b5e2b89863ec04ecdbe4fb92e709d80c69`。

## EXP-025：BTB 双端口需求改用双 LUTRAM IP 副本

- 日期 / 负责人 / 状态：2026-07-18；`btb_lutram_vivado` subagent 负责正式 Vivado 门禁。功能专项和完整性能已由主代理通过；canonical cold 280 MHz implementation 完成，但 post-route WNS 仍为负，候选时序失败，不生成 bitstream、不上板。
- 假设 / 实现：用两个相干的 `dist_mem_gen_32x32` 简单双端口异步读副本替换 BTB 的 `Reg(Vec)`：一个服务 fetch query，一个服务 update RMW；16-bit resettable valid mask 屏蔽未初始化 LUTRAM。`BranchTargetBuffer.scala` SHA-256 为 `152492ccf91d4968ba7a2503fbdb014f1a3c8e24b444189c0154c418e03168a0`。综合最终 mapping 明确出现两份 32x32 `dist_mem_gen_32x32`，每份使用 6 个 `RAM32M`。
- 性能：完整主样例为 `460,174,656 cycles / 380,344,384 instructions`，与 v6 完全一致；280 MHz 理论时间 `1.643481 s`。因此本实验只检验以 Vivado LUTRAM IP 降低寄存器/逻辑资源是否改善物理时序。
- cold / 输入冻结：旧 `.Xil/.cache/.gen/.runs` 和旧 imported synth DCP 均移动到 `/tmp/jyd-btb-lutram-cold-20260718-014752`，未删除。`HEAD=5aaafb909485802bc572c4f00d7bbe16eff82e99`；`pack-fpga.zip=41d101a0f7dcf41c16c975b2d0a0cdaebd519fe42a3c20b47dea3f046c0d55ba`，XCI/XDC/COE manifest 分别为 `82f5aaa7dbd9acb13a91f3793f075be8c8253a3c9e692aa2b9de0a5d0f77bec6`、`4a23ad97e6900cb1cd2acd23f0af0023040003a40f9ab5315f354cd66f93b5a4`、`410e203a5bef99d4964b38997afa7a015f5782739a1b61acfca74ed701de5d1a`。正式进程前后 9 份磁盘 XCI 逐文件 SHA-256 完全一致，没有 normalization；工程实际加载并重新生成 8 个 IP，run 前后全部 unlocked。
- 工具 / 源 / 时钟：Vivado 2024.2（SW `5239630`、IP `5239520`），正式命令 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8`。工程解析 24 个 pack-fpga 源且全部位于当前仓库；`synth_1/top.tcl=6ad832959067f0cb0e66ad5c06f7dc2985ea63711fc2e4c8b0ea967376d484f9`，30 个 Verilog/SystemVerilog 逐文件 source manifest 为 `e054b96c918eaf53092748a7f36e9e4bf31f42a7fe7f63bda1613a0964520292`。PLL requested/actual 均为 `280.000000 MHz`，误差 0；VCO=1400 MHz、DIVCLK=1、feedback=7、CPU divider=5，综合传播周期 3.571 ns。没有 `Designutils 20-1307`、`Constraints 18-513` 或 error。
- 正式 directives：全量 `reset_run synth_1`，不使用 incremental checkpoint；实际命令为 `synth_design -top top -part xc7k325tffg900-2`、`opt_design`、`place_design -directive ExtraPostPlacementOpt`、`phys_opt_design -directive AlternateFlowWithRetiming`、`route_design -directive Explore`，以及脚本唯一 post-route `phys_opt_design -directive AggressiveExplore`。
- routed 时序：WNS/TNS/failing endpoints/WHS/THS 为 `-0.159 ns / -3.937 ns / 80 / +0.052 ns / 0.000 ns`。top1 是 `payloadReg_4_info_reg2_reg[1]/C -> payloadReg_4_info_reg1_reg[21]/D`，13 levels（`CARRY4=6 LUT2=1 LUT4=1 LUT5=2 LUT6=3`），data delay 3.582 ns，其中 logic 1.243 ns、route 2.339 ns（65.3%）。
- post-route 时序：唯一 `AggressiveExplore` 改善为 `-0.126 ns / -3.360 ns / 74 / +0.052 ns / 0.000 ns`。top1 为同一 `reg2[1]/C` 到 WBU `gpr_data[22]/D`，11 levels，data delay 3.499 ns（logic 1.106 ns、route 2.393 ns，68.4%）；top2 同源到 `gpr_data[14]/D` 仅 6 levels 但 route 占 86.4%。top3～10 仍是 late forwarding、late ADD、store epoch 等多族路径，不是 BTB query/update 路径。最终违例超过流程的 `-0.1 ns` 小违例边界，因此不运行 exact Auto_1 或其他策略。
- 资源对比：相对同一 v6 audit 的 placed 报告，Slice LUT `4441 -> 4271`（-170），LUT-as-logic `3907 -> 3701`（-206），LUT-as-memory `534 -> 570`（+36），Slice registers `5369 -> 4889`（-480）；BRAM tile 68.5、DSP 4 不变。IP 化确实显著减少了 BTB 寄存器和组合 LUT，但物理重排使剩余 late-result/forwarding 路径从 v6 同血统最佳 `-0.018 ns` 退化到 `-0.126 ns`。
- 产物：`synth_1/top.dcp=a60caec4519f49005fa23d518811786d800de05ad6e6e1bf899170dd5ba66a0c`；routed/post-route DCP 为 `2c527c3792e19c416d0254de17c42db5d0dde55177f59337a3a3b04c161da10d`、`b547e25883b13834a5c03140b100b52b9d3bb7895455d6cf059a0cd617a5559b`；工程 timing report 为 `5533e6abdf0034bd53ef031f2c1171b2b33fd021dfd6e01cfca01a9764508b56`、`9016312e75f2d7874632a7b7824bba7e55a5c3b30f9a98b1df33e601bc6d555b`。
- 独立 reopen：分别重新打开 routed 与 post-route DCP 后重报，数值与工程报告逐项一致。reopen timing SHA-256 为 `2fff64eadbd30775ae32a6b7e72c75a428234d0e249d04235f282a5ff9bf07bd`、`660eef0ff383444be87c10abdf15f79841c887a7ff21e599084a3bcc556f3c18`；top10 为 `0d45876092b43ec97bbb480cae5e8b7a8836c93c4033327887a5da4c905557be`、`98961b2b005d3d4b8079b652be4e6b31d86246adb4f6ca6fe57c5e97b053efc2`；reopen utilization 为 `c2e350e80118fac70f1e93050ef7b790b4bc98f2a134ad40091ca2bc7b809f2f`。
- 结论：Vivado distributed-memory IP 替换 BTB `Reg(Vec)` 在功能、周期和资源上都成立，但本次 cold implementation 没有改善 280 MHz 最坏时序，反而扰动原本敏感的 forwarding 解。该候选不能单独保留为时序优化；后续只有在快速 `mul` 等结构改变已重塑关键路径后，才值得作为资源优化重新组合验证，不能复用本轮负 WNS 结果。

## EXP-026：DCache 1-bit generation 单项（并发污染，诊断结果作废）

- 日期 / 负责人 / 状态：2026-07-18；`epoch_toggle_vivado` subagent 负责 Vivado 门禁。候选把 8-bit store epoch 缩为 1-bit toggle，并用“前一拍 store 必须使旧 refill 失效、响应同拍 store 直接压制 refill”的断言验证固定两拍 JYD DRAM 协议。格式、Verilog、五项 CPU、RT-Thread 和完整 perf 已通过；完整性能仍为 `460,174,656 cycles / 380,344,384 instructions`，280 MHz 理论时间 `1.643481 s`。
- 冻结输入：BTB 已恢复 `Reg(Vec)`，`BranchTargetBuffer.scala=42a908b2f476c1c007a4f70990fbf55a2909dca6c7f5758bf228cb9cc11fafa5` 且相对 HEAD 无 diff。四个候选 RTL `Top/exu/lsu/wbu` SHA-256 依次为 `dc04f65b...`、`dd515d94...`、`d39d2cc9...`、`88027049...`，四文件 diff 为 `9c9e3b89...`。`pack-fpga.zip=33b2e519048ef5f33fcddb5ae75f31a6d6e555cce1adb588974b05eff2098aee`，生成目录 manifest 为 `ad28a5a125cff1af7f0ad48e1ad209eb725c9177c3eff37bd7f6b3799af8736c`；固定镜像 SHA-256 仍为 `b998eae7...`。
- cold 处理：旧 `.Xil/.cache/.gen/.runs` 移至 `/tmp/jyd-epoch1-cold-20260718-021520`，未删除。第一次启动在派生顶层 synthesis 时因执行会话异常中断，未得到顶层 DCP，残留另行移至 `/tmp/jyd-epoch1-aborted-20260718-021709`。重启前 XCI/XDC manifest 为 `82f5aaa7...` / `4a23ad97...`，命令为 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8 --skip-pack`。
- 作废原因：重启的 Vivado 进程打开工程后，另一项已授权的 fast multiplier 实验在共享工程新增 `mult_gen_mul32_fast.xci` 并修改 XPR；此外磁盘 XCI 还发生 BRAM GUI 元数据规范化。脚本最终按硬门禁报告 XCI manifest 在进程内从 `2e3b42a4...` 变为 `fd7fd4f5...` 并以状态 1 退出，检查时磁盘 manifest 已继续变为 `c1d61a07...`。因此本轮不是 immutable canonical sample，全部 DCP、报告和数值只能诊断，不能归因于 epoch 候选、不能用于策略比较、bitstream 或上板。
- 诊断实现仍通过 24 个 pack-fpga 源路径门禁、8 个启动时 project IP unlocked、PLL requested/actual `280.000000/280.000000 MHz`、VCO 1400 MHz、1/7/5 分频；未见 `Designutils 20-1307`、`Constraints 18-513` 或 route error。实际 directives 为默认 `opt_design`、`place_design ExtraPostPlacementOpt`、`phys_opt_design AlternateFlowWithRetiming`、`route_design Explore` 和唯一 post-route `phys_opt_design AggressiveExplore`。
- 诊断时序：routed 为 WNS/TNS/endpoints/WHS/THS = `-0.167 ns / -3.009 ns / 46 / +0.071 ns / 0.000 ns`；post-route 为 `-0.114 ns / -1.272 ns / 34 / +0.039 ns / 0.000 ns`。post-route top1 为 `payloadReg_4_info_reg2[2]/C -> payloadReg_4_info_reg1[12]/D`，11 levels，data delay `3.654 ns`（logic `1.017 ns`、route `2.637 ns`）；top2 是同源 15-level ADD/forwarding 路径；top3/4 是 peripheral response 到 operand；其余仍为 late forwarding、late ADD 和 decode/operand 路径。该诊断没有显示 1-bit generation 自身成为 top10。
- 诊断资源：Slice LUT 4429、LUT-as-logic 3895、LUT-as-memory 534、Slice registers 5346、BRAM tile 68.5、DSP 4。诊断产物 SHA-256：synth/routed/post-route DCP 为 `e81be1eb...`、`844f0ed1...`、`7ef304df...`，routed/post-route timing report 为 `001a2e69...`、`434ae48e...`；这些哈希仅用于识别并隔离污染批次。
- 决定：不为 epoch 单项重跑，也不运行 Auto_1 或其他策略。后续直接把已通过功能与性能门禁的 1-bit generation 纳入 fast-mul/divider 组合，重新冻结全部 RTL、pack、XPR、XCI、XDC 和 COE 后做新的 canonical cold run。

## EXP-028：Divider Generator CPD=4 组合前门禁

- 日期 / 状态：2026-07-18；在已通过功能门禁的 1-bit generation + fast/slow multiplier 共享候选上完成 RTL、IP OOC 和完整性能验证；本节不包含顶层 implementation 结果。
- IP 配置：保持同名 `div_gen_uradix2`、Radix2、unsigned 32/32、Remainder、NonBlocking、Performance 和 Automatic latency，只把 `clocks_per_division` 从 1 改为 4。Vivado 2024.2 在 `xc7k325tffg900-2` 上生成的实际参数为 `DIVCLK_SEL=4`、`C_LATENCY=35`；原 CPD=1 为 34 cycles。
- RTL 同步：增加 `DividerConfig.latency=35`，inline 仿真模型的 valid/data pipe 深度、循环上界和输出索引全部由该常量派生；Divider FSM 仍只等待 `m_axis_dout_tvalid`，接口和单 outstanding 语义不变。CPD=4 生成的 input AXIS 自动带 `tready` 输出，但连续请求最短间隔为完整 35-cycle 操作，远大于四拍接受间隔，因此无需把 ready 引入 CPU FSM。
- 独立 OOC 探测：470 Slice LUT、1302 Slice registers、0 DSP；对照 CPD=1 为 1185 LUT、3269 registers、0 DSP，即减少 715 LUT（60.3%）和 1967 registers（60.2%）。综合 DCP 施加 3.571 ns 约束得到 WNS `+1.392 ns`、TNS 0；该数值仅作为 OOC 可行性指标，不代替顶层 routed timing。
- 性能：完整 `withMext-v2.bin` 正常结束于 `449,678,662 cycles / 380,344,384 instructions`，相对同一 fast-mul CPD=1 候选的 `449,678,652` 精确增加 10 cycles，证明 16 条 div/rem 中 10 条走普通 IP、6 条走除零/溢出 special bypass。280 MHz 理论时间为 `1.606 s`，明显低于 1.70 s；报告目录为 `npc/history_perf/5aaafb90/0717T18_50_36.412519866/`，report/raw JSON SHA-256 为 `82a26a6925f0fc036abe9b70f41e32f436ea128699056d84663dae3df9434f26`、`5d7353b4493eb8bf415ca993e6f2d5e12c50711504089076fa510e87c5694be4`。
- 验证：`reformat`、`checkformat`、`verilog` 和五项 CPU（add/load-store/switch/if-else/recursion）通过；div/divu/rem/remu 四项定向架构测试全部通过；fast/slow multiplier 的 mul/mulh/mulhu/mulhsu 四项回归保持通过。`verilog-lint` 只有仓库允许忽略的 7 条 `PINCONNECTEMPTY` 后因 warning-fatal 退出；`git diff --check` 通过。
- 冻结身份：`alu.scala=61dba4d1e097eeb9a6d11f41740bd37f2e64c28630ec03c3cedc1b9f5776b7a3`；canonical XCI `72317691f52fa8aec1b656ea2f2953e956137936b8ce89f929d36989c504ead4`；生成 synth VHDL `02f2f44c9ae871ba9afcc639a99b5bc71d6a1e6e3038212dd422a5d5b02e2b02`；OOC DCP `c18cce0f4052d0e611e2e7285deb3139ad97f8e486a3f00fb46a9eaa432ea56c`。
- 决定：CPD=4 以可忽略的 10-cycle 性能代价换取约 60% divider LUT/FF 降幅，满足进入 1-bit generation + fast/slow multiplier + divider 组合 canonical cold 280 MHz implementation 的条件；只有该不可变组合的顶层 post-route WNS 非负时才可验收。

## EXP-029：1-bit generation + fast/slow multiplier + CPD=4 divider 正式组合

- 日期 / 负责人 / 状态：2026-07-18；`combo_280_vivado` subagent 只负责冻结输入、canonical cold implementation、唯一候选特定策略比较和独立 reopen。组合功能与完整性能门禁已在 EXP-028 通过；正式 post-route 仍为负 WNS，候选时序失败，不生成 bitstream、不进行上板。
- 候选 / 性能：BTB 已回退 v6 `Reg(Vec)`；保留 DCache 1-bit generation、普通 `mul` 专用 32x32 low-32 latency-3 IP、高位乘法 latency-4 IP，以及 CPD=4/latency-35 divider。完整性能为 `449,678,662 cycles / 380,344,384 instructions`，280 MHz 理论时间 `1.6060 s`，周期目标有充分余量。
- 冻结 / cold：先运行 `make -C npc pack-fpga`；`pack-fpga.zip=a908d70f84f07ba93948dee762a42c29e2ff95ef0916b9a043ec075bb3de7b25`，包含 24 个源。XPR 为 `9c66d24d3fb3915cbbccc5a91a3fc0cef48d62e0cb74328c17788f56addffec3`。旧 `.Xil/.cache/.gen/.runs` 安全移至 `/tmp/jyd-combo280-cold-20260718-030349`，未删除。正式命令为 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8 --skip-pack`。
- IP 门禁：Vivado 工程实际 `get_ips` 为 9 个，run 前和 output product/OOC DCP 重建后全部 unlocked；磁盘另有一份未被工程加载的 `pll_1.xci`，所以冻结清单含 10 份 XCI。进程前后 10 份 XCI 逐字节完全一致，没有 canonicalization。divider 生成 HDL 为 `C_LATENCY=35 / DIVCLK_SEL=4`；fast multiplier 为 `C_LATENCY=3 / C_OUT_HIGH=31`，OOC mapping 使用 3 DSP；slow multiplier latency 4，使用 4 DSP。divider/slow/fast OOC DCP SHA-256 分别为 `63db24858dca76786bb17f3bf00ef5080652ad5b00824aaba8e9addd82387d26`、`d88e8453b82fc137e44caca550b87413f53070802249c7061250e79ae01cb439`、`b5ba7be9cfc3355d6abc2a4b928c32c82e126daf8e10e2752088c9ab86e1c7f3`。
- 工具 / 源 / 时钟：Vivado 2024.2（SW `5239630`、IP `5239520`）；24 个 pack-fpga 文件全部解析到当前仓库。`synth_1/top.tcl=63594ffa2bbb6279b0fb5f13a06176802043ed5db570b083e9218629a7b3f0ab`；29 个直接读取的 Verilog/SystemVerilog 文件 manifest 为 `67920a150e095021a903938219ddcf240fe50f7e2a4d1c8deda75261914278f0`。XCI/XDC/COE manifest 为 `a74e16eb838d84d39f2d052ba17a4e7337aae2493e558a25b648b2d6db7b6fac`、`96a2b377c9899f5eceb02af4f28a30675432183484d430b13db9d75bf2cb3710`、`3171a4d60237d116eecda3ed7373f032020c474f8604788a028a321b3fed28f6`。PLL requested/actual 均为 `280.000000 MHz`，误差 0；VCO 1400 MHz、DIVCLK 1、feedback 7、CPU divider 5，综合传播周期 3.571 ns。未见 `Designutils 20-1307`、`Constraints 18-513` 或实现 error；`my.xdc` 中计数器对象不存在的既有 false-path warning 仍存在。
- 正式 directives：全量 `reset_run synth_1`，无 incremental checkpoint；实际命令为 `synth_design -top top -part xc7k325tffg900-2`、`opt_design`、`place_design -directive ExtraPostPlacementOpt`、`phys_opt_design -directive AlternateFlowWithRetiming`、`route_design -directive Explore`，以及唯一 post-route `phys_opt_design -directive AggressiveExplore`。
- 正式时序：routed WNS/TNS/failing endpoints/WHS/THS 为 `-0.134 ns / -2.039 ns / 54 / +0.073 ns / 0.000 ns`；post-route 改善为 `-0.084 ns / -1.112 ns / 33 / +0.073 ns / 0.000 ns`。post-route top1 `reg2[2]/C -> reg2[14]_replica/D` 为 7 levels、logic/route `0.524/3.048 ns`（route 85.3%）；top2 `reg1[9]/C -> reg2[3]/D` 为 8 levels、`0.567/3.047 ns`（84.3%）；top3 `ID/EX code[2]/C -> reg2[10]_replica/D` 为 5 levels、`0.530/2.964 ns`（84.8%）；top4 到 WBU `gpr_data[31]/D` 为 10 levels/6 CARRY4、`1.064/2.353 ns`；top5 `reg2[3]/C -> validReg_3/D` 为 9 levels/4 CARRY4、`0.864/2.633 ns`。违例主要来自 operand/forwarding/redirect 的物理分散和 late ADD，不是 1-bit generation 或单一 rs2 late-add 路径。
- 正式资源 / 产物：placed Slice LUT 3742、LUT-as-logic 3216、LUT-as-memory 526、Slice registers 3176、BRAM tile 68.5、DSP 7。`synth_1/top.dcp=6a614db90b3cb136080bddfbeda36e2bd8933b00d5e00139ff40bbf7a137ab03`；routed/post-route DCP 为 `ab18d187742e0300f6c3bac7ccc6054c912b5c85052efa1d7d36f6c195ea9c5f`、`123c943448d83b30d3b4590eb2ad01fa96982b1d7052ff6ac2907fc23be721c1`；工程 timing report 为 `e98d8f7434ce91e46af3128083ffdba65a2e4676e36cea6f45cb1f7fd6c6fa62`、`64e5b3d817337c22d9effbdd1f8872d4cfd9462648ea6e243f610e417f3298c3`。
- 唯一策略比较：严格固定上述 synth DCP，不重新综合、不覆盖正式 `impl_1`，产物在 `/tmp/jyd-combo280-auto1-20260718`。实际命令仅为 `opt_design -directive Explore`、`place_design -directive Auto_1`（Vivado 明确因 7-series 降级为 Explore）、`phys_opt_design -directive AggressiveExplore`、`route_design -directive NoTimingRelaxation` 和唯一 post-route `AggressiveExplore`。routed 为 `-0.177/-2.767/50/+0.073 ns`，post-route 为 `-0.176/-2.759/50/+0.073 ns`，明显差于正式策略；不再扩展策略。Auto_1 routed/post DCP 为 `9c37fd3ff39ff134ca02141f25559b08a675482853250b8f9c36cfc6b6d021b9`、`a762fb1c32f22a07248def810a2a367a6e8c73ac03ab411f342aa8ba3beb5428`，timing report 为 `257c53edb7f0693597d2e8b9be505e1ab6e75cfaff4a2ea038b50ea8c32bf235`、`e8154e12edda61ab7d300cb0dee94a9a36c650e846c7a2d66c56c8b02bb17735`。
- 独立 reopen：在 `/tmp/jyd-combo280-reopen-20260718` 分别重新打开正式 routed/post-route 与 Auto_1 routed/post-route 四个 DCP，全部逐项复现原 WNS/TNS/endpoints/WHS/THS。四份 reopen timing SHA-256 依次为 `83390bc2cf1f8e76b438e9e7ccfe9759094fccf58491bc2ea7311f09a63b0444`、`3c1aad91f39bf84bdc284e45991d7da17a6cce0aae7caee79cfa93d0a8c1dfb7`、`af6b3cd891773e43b2365d7bbe3e2c293895c09e1fc813ee2d14456287a86ee2`、`6c7cd1dad8f8c2f5bd41bf18891ecfcada33018f3ecf2d91546e5d7761567070`。
- 结论：该组合用 fast multiplier 和 CPD=4 divider 同时取得 `1.6060 s` 理论性能及显著资源下降，但正式最优仍为 WNS `-0.084 ns`，不能作为 280 MHz 最终候选。有限预选策略已耗尽，不生成 bitstream、不上板；下一结构实验应优先减少 operand/forwarding/redirect 的跨区连接，而不能把问题归因于 store generation 或继续随机扫描实现策略。

## EXP-030：撤掉 full-word store write-allocate 的性能交换门禁

- 日期 / 状态：2026-07-18；在 EXP-029 的 1-bit generation + fast/slow multiplier + CPD=4 divider 正式组合上，只撤掉 commit `196077b` 引入的 full-word cacheable store write-allocate；本节只完成 RTL、功能和性能门禁，不启动 Vivado。
- 实现：所有 cacheable store 都在 `memReqFire` 时 invalidate 当前 tag；删除 `storeUpdate/storeData/storeMask` 跨 EXU/Top/DCache 接口以及 tag、data BRAM、四个 late-data shadow RAM 的 store 写选择。DCache data/shadow 现在只由 WBU load fill 更新。任意 cacheable store 继续 toggle 1-bit generation；同拍 store 仍优先压制旧 fill，并保留“前一拍 store 必须使 refill 失效”和 store/fill 互斥断言。branch predictor、fast/slow multiplier、CPD=4 divider 及其他组合改动均未改动。
- 完整性能：固定 `withMext-v2.bin`（SHA-256 `b998eae7b2e6c3dd5911bc67927cea4e9c4f8a1bcb639391c520ba3391742749`）自然正常结束于 `470,769,364 cycles / 380,344,384 instructions`，CPI `1.2377`；280 MHz 理论时间 `1.68132 s`。相对 EXP-029 的 `449,678,662` 增加 `21,090,702 cycles`，但仍低于 `476,000,000 cycles` / `1.70 s` 硬门槛，因此该性能交换通过并可进入独立 canonical cold implementation。
- 动态计数：`mul=10,496,004`、`mulh=128,004`、`mulhsu=4`、`mulhu=18`，div/rem 总计 16；cacheable full-word store `21,450,238`。计数与前序候选一致，性能退化量比 full-word store 数少 359,536，符合部分原 write-allocate 并未节省后续 miss 的预期。为避免被优化掉的内部节点导致仿真器编译失败，仿真计数器改用等价且仍存在的 `io_dcache_invalidate && memWMask==0xf`，不增加 FPGA RTL 信号。
- 验证：`reformat`、`checkformat`、`verilog` 通过；`verilog-lint` 只有仓库允许忽略的 7 条 `PINCONNECTEMPTY` 后 warning-fatal 退出。`add/load-store/switch/if-else/recursion`、RT-Thread 到 `msh />`、`mul/mulh/mulhu/mulhsu/div/divu/rem/remu` 全部通过，所有 generation/fill 断言保持安静；`git diff --check` 通过。
- 产物身份：`Top/dcache/exu/alu/lsu/wbu` SHA-256 依次为 `dfa641df23082c4039a5008adc00a2d914d5de54e2906330dacf847e01a2769a`、`a3dfc10417ce9f7be60da2a2bf0b2440968d453a6d7c47f701e9c5587d23e945`、`6502fcb85f8fdeef5067326a5f6ffd9289004dd1ac2ac59cb451c51584f2c58c`、`61dba4d1e097eeb9a6d11f41740bd37f2e64c28630ec03c3cedc1b9f5776b7a3`、`d39d2cc9399eeecfb049946e080d972a8f1ff0a029ad55e6bc9e0c794cb9c8e3`、`88027049dc90cfc88862de2483f596710ae7c6dee05e48de4aa0b8a5a26bf4b5`；生成 `CPUTop.v=c0af738c49074aace97c344dd82644df78830308000d988d01d34bc01cb09054`。divider/fast-mul XCI 仍为 `72317691f52fa8aec1b656ea2f2953e956137936b8ce89f929d36989c504ead4` / `b7de6c6a6cf6a7d72696bd26feca0c082934f56933973aa48ffd866b882e19e1`。
- 原始性能报告目录为 `npc/history_perf/5aaafb90/0717T19_17_51.056013833/`；report/raw JSON SHA-256 为 `9e1a56091b2315d7939c309ddcadaf812d06da2bd6cf46f228e2af9946cfa45b` / `76f71abf993fa60fac7a0abf29bb98535e836f7a57935f463f1e11a3e09f592e`。决定：保留该候选，下一步只允许以这一冻结组合做新的 canonical cold 280 MHz implementation，不复用 EXP-029 的 DCP 或时序结论。

## EXP-031：撤销 store write-allocate 组合的正式 280 MHz 实现

- 日期 / 负责人 / 状态：2026-07-18；`no_store_alloc_vivado` subagent 只负责冻结输入、canonical cold implementation 和独立 reopen。候选功能与完整性能门禁见 EXP-030；`470,769,364 cycles / 380,344,384 instructions` 在 280 MHz 下理论为 `1.681319 s`，性能合格，但正式时序失败。
- 冻结 / cold：先刷新 `pack-fpga` 和工程 imported RTL；`pack-fpga.zip=aa7756a49cbfa066f4989fc0c319656a15043dfb7b990a882ff5f576faa6bea9`，XPR=`5977299754c8f2a045f8b0df9966f383031c6ed555f86e84f7deae0bc4df446f`，29 个直接读取的 Verilog/SystemVerilog 文件 manifest=`49681eacceb9b4ac45de5dd03e48538ed1c737d583c8d9cfaa73d2c256105cd4`。旧 `.Xil/.cache/.gen/.runs` 安全移至 `/tmp/jyd-exp030-cold-20260718-032941`，未删除；`HEAD=5aaafb909485802bc572c4f00d7bbe16eff82e99`。
- IP / 时钟：工程实际加载 9 个 IP，磁盘另有未加载 `pll_1.xci`；run 前后 10 份 XCI 逐文件不变，aggregate=`a74e16eb838d84d39f2d052ba17a4e7337aae2493e558a25b648b2d6db7b6fac`，loaded IP 全部 unlocked。生成参数为 divider `C_LATENCY=35 / DIVCLK_SEL=4`、fast multiplier `C_LATENCY=3 / C_OUT_HIGH=31`、slow multiplier latency 4。PLL requested/actual 均为 `280.000000 MHz`，VCO=1400 MHz、DIVCLK=1、feedback=7、CPU divider=5，综合传播周期 3.571 ns。
- 工具 / directives：Vivado 2024.2（SW `5239630`、IP `5239520`），正式命令 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8 --skip-pack`；`synth_1/top.tcl=63594ffa2bbb6279b0fb5f13a06176802043ed5db570b083e9218629a7b3f0ab`。全量 `reset_run synth_1`，实际为默认 `synth_design/opt_design`、`place_design ExtraPostPlacementOpt`、`phys_opt_design AlternateFlowWithRetiming`、`route_design Explore` 和唯一 post-route `phys_opt_design AggressiveExplore`；没有 `Designutils 20-1307`、`Constraints 18-513` 或 implementation error。
- 时序：routed WNS/TNS/failing endpoints/WHS/THS 为 `-0.302 ns / -20.585 ns / 207 / +0.079 ns / 0.000 ns`；post-route 为 `-0.232 ns / -19.494 ns / 204 / +0.079 ns / 0.000 ns`。post-route top1 `reg2[3]/C -> reg1[25]/D` 为 8 levels、logic/route `0.725/2.958 ns`（route 80.3%）；top2 `redirectPendingReg/C -> pc[3]/D` 为 9 levels、`0.924/2.731 ns`；top3 到 `reg2[1]/D` 仅 6 levels但 route 为 3.107 ns（86.6%）。top10 同时覆盖 forwarding、WBU、PC enable 和 redirect target，撤销 store write-allocate 没有消除 operand/redirect 的物理分散。
- 资源 / 产物：placed Slice LUT 3781（logic 3255、memory 526）、Slice registers 3165、BRAM tile 68.5、DSP 7。synth/routed/post-route DCP SHA-256 为 `6500e3192bcb04c3f97b2436ceb2ec98991da757a6fde4bc767266f29812ac0c`、`12a34b34d763fa33f112a0d6a923b063d698471f010e81880ad3d2a542c5e108`、`39241abbecc3b65b07fa6d3c5aaa23d02810ea825400501b528307a6375cad37`；工程 timing report 为 `a00207b3358a55a731fad8e41255ec443a355ab6665037837194958c0e7cad2f`、`63b3244324a6ceb0d70b2d73980f2ec2f39ade1b31abdc25478a56cd5b0df4a0`。
- 独立 reopen 精确复现两组 WNS/TNS/endpoints/WHS/THS；reopen timing SHA-256 为 `c50cea14e818833b221f094940ca879150802ddec48cc08447b7d81b538238ba`、`3cd518c802a2755ca19e327bb0c1f8d2b3b909b473e28ec7a6e08b8f6055088c`，top10 为 `cc37807f2fd167180b97b17a96ca0bab0dda12ffd13c0b348c53e17a85a8de5b`、`b09e8b57304296a056d8c702690ecf8a1d74d1371b9e26656e3fd23a8a73529d`。
- 决定：post-route `-0.232 ns` 超出 `-0.1 ns` 小违例边界，因此不运行 exact Auto_1、不生成 bitstream、不上板。该性能交换虽然达到 1.70 s 理论目标，但时序显著差于 EXP-029，不能作为 280 MHz 候选，不继续扫描策略。

## EXP-032：只关闭 rs2 late-add 同拍旁路的性能交换门禁

- 日期 / 状态：2026-07-18；从已恢复的 EXP-029 组合出发，保留 full-word store write-allocate、DCache 1-bit generation、fast/slow multiplier、CPD=4 divider 和 v6 `Reg(Vec)` BTB，只实施计划中的第二个且最后一个性能交换。本节完成 RTL、功能和完整性能门禁；Vivado 由独立任务在输入冻结后运行。
- 实现：`CacheAwareByPassMux` 增加 elaboration-time `Boolean` 参数；rs1 静态传 `true`，保留专用 `lateAddFwd` 同拍通道；rs2 静态传 `false`，生成 RTL 中完全不引用 `lateAddFwd.valid/data`，没有增加动态 decode gate 或 mux。生成的 `ByPassMux` 只剩一套 `lateAddSelect`。仿真 PerfCounter 将已经不存在的 rs2 late-add select 视为编译期 `false`，保持 JSON/report 字段兼容且不增加 FPGA 信号。
- 完整性能：固定 `withMext-v2.bin`（SHA-256 `b998eae7b2e6c3dd5911bc67927cea4e9c4f8a1bcb639391c520ba3391742749`）自然正常结束于 `470,547,524 cycles / 380,344,384 instructions`，CPI `1.2372`；相对 EXP-029 的 `449,678,662` 精确增加 `20,868,862 cycles`，与 EXP-024 计得的 rs2-only late-add 后继次数完全一致。280 MHz 理论时间为 `1.680527 s`，低于 `476,000,000 cycles` / `1.70 s` 门槛，因此性能交换通过。
- 验证：`reformat`、`checkformat`、`verilog` 通过；`verilog-lint` 只有仓库允许忽略的 7 条 `PINCONNECTEMPTY` 后 warning-fatal 退出。`add/load-store/switch/if-else/recursion`、RT-Thread 到 `msh />`、`mul/mulh/mulhu/mulhsu/div/divu/rem/remu` 全部通过；覆盖 late-load rs1/rs2 的 `late-load` 专项通过（435 cycles）。`git diff --check` 通过。
- 产物身份：`idu.scala=8effb44f2a5ec3962d4a35e1780d2d6663d5799e936151d33355f9a838151c4e`，`PerfCounter.cpp=d13b36d628d58b7807430bed23007499d155617fa5a54a3adacaa843a4580987`，生成 `CPUTop.v=566bc1d7acef7ae1dcf671b0ef3946f3d2aaec8b61b15c4cd767ce3d3fe01ce1`；两份源码 diff SHA-256 为 `358119f2b65dee2d21066c6549599f19c297d5117496bcee2bf29801479acb23`。
- 原始性能报告目录为 `npc/history_perf/5aaafb90/0717T19_43_01.883440513/`；report/raw JSON SHA-256 为 `a861519280c32db8ec041df6f495f051dba03141d806105950cbd487be33c4cf` / `d65f5b50a350df6f4dd8d39332c9faf8ecbceeea6c140de2fd5207c39896dfff`。决定：保留该候选并立即冻结当前输入进入独立 canonical cold 280 MHz implementation；不与 EXP-030 的撤销 write-allocate 叠加。

## EXP-033：rs2 late-add 静态关闭组合的正式 280 MHz 实现

- 日期 / 负责人 / 状态：2026-07-18；`rs2_trade_vivado` subagent 只负责冻结 EXP-032 输入、canonical cold implementation、唯一 post-route 恢复和独立 reopen。完整功能与性能门禁见 EXP-032；`470,547,524 cycles / 380,344,384 instructions` 在 280 MHz 下理论为 `1.680527 s`，性能合格，但正式时序失败。
- 冻结输入：先刷新 `pack-fpga` 和工程 imported RTL；`pack-fpga.zip=2332ced59e934076c288248504acdf4ad9a6c4cc4bd601bd3204966d18740732`，24 文件 manifest SHA-256=`173780758dd68ca103530902adc5a636a9a7cc0fe00505e53beb7d837e379566`，XPR=`5977299754c8f2a045f8b0df9966f383031c6ed555f86e84f7deae0bc4df446f`。`idu.scala=8effb44f2a5ec3962d4a35e1780d2d6663d5799e936151d33355f9a838151c4e`；其余组合关键源 `Top/alu/exu/lsu/wbu` 分别为 `dc04f65b.../61dba4d1.../dd515d94.../d39d2cc9.../88027049...`。冻结 diff 与完整关键输入哈希清单归档在 `/tmp/jyd-rs2trade280-20260718/`。
- IP / 时钟门禁：工程解析 24 个 pack 源，实际加载并重建 9 个 IP，启动和重建后均 unlocked；磁盘 10 份 XCI 的逐文件 manifest 在进程前后字节相同，aggregate=`a74e16eb838d84d39f2d052ba17a4e7337aae2493e558a25b648b2d6db7b6fac`。fast multiplier 为 `C_LATENCY=3/C_OUT_HIGH=31`，slow multiplier 为 latency 4，divider 为 `C_LATENCY=35/clocks_per_division=4`。PLL requested/actual 均为 `280.000000 MHz`、误差 0，VCO=1400 MHz、DIVCLK=1、feedback=7、CPU divider=5。
- 工具 / directives：Vivado 2024.2（SW `5239630`、IP `5239520`），正式命令 `./npc/scripts/run_digital_twin_vivado.sh impl --jobs 8 --skip-pack`；`synth_1/top.tcl=63594ffa2bbb6279b0fb5f13a06176802043ed5db570b083e9218629a7b3f0ab`。全量 `reset_run synth_1`，无 incremental；实际为默认 `synth_design/opt_design`、`place_design ExtraPostPlacementOpt`、`phys_opt_design AlternateFlowWithRetiming`、`route_design Explore` 和唯一 post-route `phys_opt_design AggressiveExplore`。没有 `Designutils 20-1307` 或 `Constraints 18-513`。
- 正式 routed：WNS/TNS/failing endpoints/WHS/THS 为 `-0.242 ns / -5.838 ns / 95 / +0.069 ns / 0.000 ns`。top1 `payloadReg_5_isLoad/C -> reg1[31]/D` 为 14 levels（8 CARRY4），data delay `3.633 ns`，logic/route=`1.279/2.354 ns`；top2 到 rs2 replica 为 8 levels、route 占 74.7%；其余仍混合 operand、late ADD、PC/redirect 路径。
- post-route 恢复：正式 run 的唯一 `AggressiveExplore` 在把估计 WNS 改善至约 `-0.211 ns` 时发生 Vivado SIGSEGV。按既有工具故障规则，不重跑综合/布局/route、不增加策略，只从正式 `top_routed.dcp` 独立启动并恢复同一个 post-route pass 一次；成功结果为 `-0.126 ns / -3.796 ns / 79 / +0.051 ns / 0.000 ns`。top1 `pc[4]/C -> pc[9]/D` 为 8 levels，data delay `3.653 ns`、route 占 72.7%；top2 到 rs2[0] 仅 6 levels但 route 占 86.1%；top3 为 12-level late-add/operand 路径。违例仍覆盖 PC、rs1/rs2 和 peripheral forwarding，不是单一 rs2 late-add 数据 mux。
- 资源 / 产物：Slice LUT 3876、Slice registers 3168、BRAM tile 68.5、DSP 7。synth/routed/recovered-post DCP SHA-256 为 `1a96a388c3cc57b0cc6f3dfd184de06e1b239c24dedc27afab00bda7d593267f`、`c64f1f8f8c89490521ca918087f0498a1480c69f431640ab9ffb3b935ce4c2c7`、`03d985f1d024268409260431ffccf6cd69bf1774c6dfc80fab213c9ec04cdb28`。独立 reopen 精确复现 post-route 数值；reopen timing/top10/utilization SHA-256 为 `4ae67198bdab7a3d68d3a382b67befdb5e383260a28c78bb885bf373cd7932af`、`199f3841bb5c6101e41d7ec03bf3c19fc8852f36a2609c0444b1f1c0c6531c7c`、`cb1b8582c7a0f6707d4490b5437e859518cce600a6c75baf5b0e8da54ba3e2a6`。
- 决定：post-route `-0.126 ns` 超出流程的 `-0.1 ns` 小违例边界，因此不运行 exact Auto_1、不生成 bitstream、不上板。关闭 rs2 late-add 虽把理论时间保持在 1.70 s 内，却使正式时序明显差于 EXP-029 的 `-0.084 ns`，不能作为 280 MHz 最终候选；计划中的两个性能交换均已失败，不继续随机扫描实现策略。
