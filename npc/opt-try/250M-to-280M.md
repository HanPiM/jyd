250Mhz -> 300Mhz

如果reg1AddImm 不使用EXU数据，还是会卡在 BRAM 到 LSU 输出寄存器的路径上 -0.658 的违例

尝试启用BRAM输出寄存器，变成2周期延迟，在WBU才拿到数据，关键路径变到BTB的tag比较上 -0.627

尝试不比较tag的最高8位"h80"，关键路径变成了

- EXU到IDU的旁路 -0.405
- 除法器IP的输入 -0.377
- BTB 选出后得出nxtPC -0.368

将IDU中对EXU的旁路改到EXU中进行（旁路LSU），关键路径变成了 EXU 更新 BTB？？？ -0.854
怀疑是其他地方的时序压力导致的布线问题，为除法器输入打拍，结果变成了 -1.122 ？？？
revert 了除法器的改动，尝试减少 BTB target 的位数，去掉高 8 位 (0x80) 和低 2 位 (0x00)
-1.1
发现是EXU打出的更新BTB的路径使用了比较的是否taken，实际上更新btb不需要实际的taken信息，
WNS变成 -0.544 在PC的nxtPC上
revert了BTB target的改动 WNS -0.572，于是reapply了.

尝试调整了一些 predWrong 的计算到 IDU ，结果 nxtPC
logic delay 下降了，但是 net delay 大幅增加，WNS 变成了 -0.774

考虑换回在 IDU 进行 EXU 旁路 WNS -0.433

尝试减少 BP 中 pc+4 计算的位数去高8，WNS -0.374 为BRAM 的delay 高

尝试优化 EXU 中访存 WMask 的生成 WNS -0.44 到了一个 reg2 的EXU旁路上

尝试更换 BRAM 输出寄存器配置 Core -> Primitive WNS -0.665 在 BRAM 输出至旁路

尝试把BTB更新信号打一拍 -0.376

恢复原先的 WMask 生成，WNS -0.467 变成了 pc 的更新

---

尝试减少 exu nxtpc mux 的位数(->22) **WNS -0.300**

- BRAM 路径 -0.300
- EXU 到 IDU 旁路 -0.267
- pc 更新 -0.253

尝试将 DRAM 的大小从 65535 -> 65536 WNS -0.300 无区别

尝试使用优化的 WMask WNS -0.55 来自 BRAM

(x) 换用 Performance_WLBlockPlacement WNS -0.507 换回 Auto_1

(x) 尝试在 IDU 计算 WMask WNS -0.400，撤回
(x) 尝试在 IDU 选出 WData WNS -0.512，撤回

(x) 尝试 BTB 数量改为 8 WNS -0.449 
(x) 尝试 BTB 数量改为 32 WNS -0.489

---

尝试优化 perip 桥判定外设映射的逻辑，只判定关键位 WNS -0.339

尝试在此基础上使用优化的 WMask 生成 **WNS -0.268**

- pc 更新 -0.268
- BRAM 输出到旁路 -0.232
- EXU 旁路 -0.232

考虑再砍掉 reg1AddImm 的最高2位，因为 0x801/802/803 的高10位是一样的
WNS -0.389

考虑再砍掉 pc+4 计算的2b WNS -0.302

考虑再砍掉exu nxtPC mux 的2b WNS -0.317

考虑再砍掉 Top 中 pc 更新的2b WNS -0.317

考虑 idu 中 reg1AddImm Mux 只mux 22位 WNS -0.317

换用 Performance_NetDelay_high WNS -0.365

换回 Auto_1

将 redirectNow 的 valid 使用 in.valid 而不是 out.valid WNS -0.325

恢复idu中 reg1AddImm Mux 的位数 WNS -0.325 TNS-20.944

换 Performance_WLBlockPlacement WNS -0.307 TNS -35.941

换回 Auto_1 使用 Vivado 2025.2.1 **WNS -0.253**??? TNS -17.142

使 idu 中 reg1AddImm Mux 的位数回到 22b WNS -0.253 TNS -17.142

(x) 尝试 rs2 noneed的 mux 放在 bypass 之后而不是读取之前 -0.378 TNS -51.697

(x_1) 修剪 idu 中 staticNxtPC 的位数 -> 20b WNS -0.314 TNS -18.948
修剪 BTB target 的位数20b WNS -0.314 TNS -18.948

(x) 为除法器的输入打拍 -0.497 

(x) 尝试把 reg1AddImm 的计算放在 exu 中 WNS -0.826 来自 BRAM 输出...

revert (x_1) -> WNS -0.253 TNS -17.142

使用 Performance_Explore WNS -0.217 TNS -13.517
使用 Performance_ExplorePostRoutePhysOpt WNS -0.191 TNS -18.162

(x) 尝试提前在 idu 选出部分 WrBack 数据 WNS-0.340


Vivado 2025.2.1 生成的比特流似乎无法上板正确运行，换回 2024

修复 BRAM 的 ena 问题，ena 赋值恒真，在 275Mhz 下 -0.1 的违例，上板测试
得到当前（reg1AddImm 阻塞EXU）
通过对比 8.706s -> 8.059s 相比于 250Mhz -> 275Mhz 的理论提升
的 CPI 大概是

1.5353 * (8.059/8.706) / (250/275) = 1.5353 * 1.01825 = 1.563

增加了 0.03 的 CPI，用此估算达到 300Mhz 时的理论性能是 7.387s

300Mhz 下当前 WNS -0.342 TNS -25.81

尝试把 irom 的 ena 也恒真，结果 WNS -0.355 TNS -17.877

(x) 尝试把 EXU 的旁路挪到 EXU 旁路 LSU WNS -0.605 TNS -126.383，主要在 pc 寄存器的更新条件

尝试把分支的 func3t mux 改为在 IDU 生成独热信号，再在 EXU 进行 mux 比较结果 WNS -0.392 TNS -50.122 主要在pc 寄存器的更新CE 的 net delay 上

(x2) 考虑允许分支预测错误的更新打一拍延迟到LSU更新，WNS -0.175 TNS -8.691 Explore 优化下 WNS-0.187 TNS -10.650 Retiming 优化下 WNS -0.219 TNS -20.634
该违例下性能为 7.732s，对应 CPI 为 1.5353 * (7.732/8.706) / (250/300) = 1.5353 * 1.066482 = 1.637

(x) 尝试将 EXU 旁路挪到 EXU 旁路 LSU WNS -0.285 TNS -29.578

修剪 BTB 的 target 位数到 17b （因为跳转目标只能是 IROM/DRAM 且地址范围最大256K)
WNS -0.334 ns TNS -22.297 ns ???

(x) 再尝试把 reg1AddImm 的计算放在 exu 中 WNS -0.585 ns TNS -232.962 ns 来自 BRAM 输入

修剪 reg1AddImm 的加法器位数 WNS -0.379 ns TNS -100.222 ns 来自 PC 更新 CE 的布线？？？

尝试 revert 分支预测(x2)的打一拍 WNS -0.273 ns TNS -17.401 ns 来自 PC 更新 CE 的布线

Explore 优化下 WNS -0.236

尝试修剪top 中 pc 的mux位数 (Explore 优化下)WNS -0.334 ns TNS -17.316 ns 来自 reg1AddImm

考虑取消 reg1AddImm 的计算中对 WBU 旁路（访存）的使用 (Explore 优化下)WNS -0.208 ns TNS -36.266 ns
来自 pc 更新的数据路径的布线，该条件下 300Mhz 下性能为 7.636s 对应 CPI 为
1.5353 * (7.636/8.706) / (250/300) = 1.5353 * 1.0525155 = 1.6159

恢复 reg1AddImm 的计算中对 WBU 旁路的使用，改为只是不使用访存的结果
(Explore 优化下) WNS -0.227 ns TNS -20.421 ns
Auto_1 下 WNS -0.37 ns TNS -63.804 ns 在旁路的布线
(ExplorePostRoutePhysOpt 优化下) WNS -0.218 ns TNS -23.034 ns 在 pc 数据和布线

再次尝试在 idu 提前选出部分 WrBack 数据 (Auto_1 下) WNS -0.234 ns TNS -10.306 ns
主要在于 reg1AddImm,pc/CE 布线

Explore 优化下 WNS -0.187 ns TNS -6.218 ns 主要在于 pc/CE, reg2旁路 布线
ExplorePostRoutePhysOpt 优化下 WNS -0.16 ns TNS -6.335 ns
主要在于 reg1AddImm 送入BRAM 的布线, reg2, reg1AddImm 旁路, 

修剪 PC/BTB 的位数到 15b 旧的 pll 下是 WNS -0.35 但是设置 pll 输入 100Mhz 启用 Minimize jitter 后 WNS -0.197 ns TNS -6.167 ns ???
主要在于 reg1AddImm,reg1/reg2旁路,pc/CE 的布线
(Explore 优化下) WNS -0.138 ns TNS -3.668 ns
主要在于 BRAM 输出, reg1AddImm 的布线

(x) 把 BP 的Mux位数到 15b, (Explore 下) WNS -0.144 ns TNS -7.048 ns

(x) 尝试优化 WData 的生成 (Explore 下) WNS -0.335 ns TNS -39.066 ns

(x) 此时取消掉之前 WMask 的优化，WNS -0.269 ns TNS -12.859 ns

(x) 此时取消掉IROM的ena 恒真，WNS -0.283 ns TNS -13.791 ns

尝试优化 IROM addr 的 -addr_base -> 直接取低位 WNS -0.138 ns TNS -3.668 ns 无区别

考虑去除 IROM 对 wen 的检测，无区别

(x) 尝试去除 CSR 对 mvendor_id 和 march_id 的支持 WNS -0.374 ns TNS -54.508 ns

尝试 trim staticNextPCOrCSRTarget mux 位数 无区别

(x) 尝试在 IDU 选出生成 WMask WNS -0.141 ns TNS -1.99 ns
在于旁路和pc

(x) 此时让 BP Mux 位数为 15b WNS -0.141 ns TNS -1.99 ns

尝试让 WMask 的生成Cat 完再 OR
BP Mux 15 + 无 IDU WMask 生成 WNS -0.144 ns TNS -7.048 ns
无 BP Mux 15 + 无 IDU WMask 生成 WNS -0.138 ns TNS -3.668 ns 无区别

---

Verilog commit 7694be63e94e668e62626e3ba6c1453b00609287
Vivdaod proj commit 8dd0b077549547104a024d3e9cc49a85b685465a

280Mhz 下 Auto_1 WNS 0.013 ns 先这样吧

## 考虑在 IFU 旁路访存地址？

对于访存指令，IFU阻塞reg1AddImm的计算
